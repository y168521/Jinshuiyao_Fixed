# JS-20260806-01 · 视觉 L2 禁用色根治收尾

> 接 2026-07-28 P0（JS-20260728-18）未收尾部分 + 全项目残留禁止色清零 + 门禁防复发。

## 一、PRD（产品经理：许清楚）

### 背景
- 7-28 的 P0 只改了 `scripts/daily_fund_monitor.py` 第 556-561 行的 **CSS 变量块**，漏了模板正文 **653/691/725 行**写死的 `rgba(139,92,246)`（紫）与 `rgba(239,68,68)`（红）。
- 结果：8 月新生成的 `金水谣数据/fund_reports/2026-08-0[2-5].html` 四份日报被"污染源"再次染脏（已亲自 grep 核实）。
- 另：`frontend/` 下 9 个页面、`jinshuiyao-guide/` 下 5 个页面内联写死禁止红/绿/黄 rgba（视觉子代理报告 + 亲自 grep 双重确认）。
- `tools/closeout_gate.py` 在 2026-08-03 重构时由 `scripts/` 迁移并改写为精简版，**丢失了原来的 `[F]` 禁用色扫描门禁**，导致无法防复发。

### 问题陈述
owner 默认主题（L2）禁用色——红 `#ef4444`、绿 `#22c55e`、黄 `#eab308`、紫 `#8b5cf6`、橙 `#f97316` 及其 rgba 等价（239,68,68 / 34,197,94 / 234,179,8 / 139,92,246 / 249,115,22）——仍出现在：生成器源码、自动报告产物、前端页面。

### 范围
| 对象 | 位置 | 说明 |
|------|------|------|
| 生成器漏网 | `scripts/daily_fund_monitor.py:653/691/725` | 模板正文 rgb禁止色 |
| 自动报告 | `金水瑶数据/fund_reports/2026-08-0[2-5].html`（4 份） | 每份 3 处禁止色 |
| 前端页 | `frontend/` 下 9 个含禁止色页面 | 内联 rgba |
| 旧版前端副本 | `jinshuiyao-guide/` 下 5 个含禁止色页面 | 内联 rgba |
| 防复发 | `tools/closeout_gate.py` | 重新加回禁用色扫描门禁 |

### 非范围（合法，不碰）
- `core/agent_theme.py` 的 `FORBIDDEN_MAP` 映射表（用于转换，合法出现）
- `docs/旧版资料/`、`金水谣数据/backups/`、`_old_backups_consolidated/` 归档遗留
- `knowledge/mirofish_db*.json` 数据字段
- `金水瑶数据/log/` 经验箱/决策记录（以文本说明形式提及禁止色，合法）

### 验收标准
1. 全项目 grep 禁止色（5 种 hex + 5 种 rgba）**零残留**（排除上述合法出现）。
2. `closeout_gate` 干跑能检出**故意注入**的禁止色（门禁有效）。
3. 改动的 `.py` 通过 `py_compile`；生成器模板字符串仍合法（可正常生成报告）。
4. 零新依赖；纯颜色值替换，不动任何功能逻辑。

### 优先级
P0（已两次漏网，须一次堵死 + 加门禁防复发）。

---

## 二、架构设计（架构师：高见远）

### 配色映射（复用 agent_theme.py 语义）
| 禁止色 | 替换为（L2 允许） | 用途 |
|--------|------|------|
| 红 `rgba(239,68,68)` / `#ef4444` | 赤铜 `rgba(200,117,90)` / `#C8755A` | 负向/报错/下跌 |
| 绿 `rgba(34,197,94)` / `#22c55e` | 墨绿金 `rgba(45,139,126)` / `#2D8B7E` | 正向/成功/上涨 |
| 黄 `rgba(234,179,8)` / `#eab308` | 香槟金 `rgba(201,169,110)` / `#C9A96E` | 警告 |
| 紫 `rgba(139,92,246)` / `#8b5cf6` | 香槟金 `rgba(201,169,110)` / `#C9A96E` | 品牌强调（深色可见） |
| 橙 `rgba(249,115,22)` / `#f97316` | 赤铜 `rgba(200,117,90)` / `#C8755A` | 浅赤铜 |

### T01-T03 实现策略
- 单一 Python 脚本（真实文件系统）对目标文件做**确定性正则替换**，保留 rgba 的 alpha 值。
- **先 dry-run**：统计每文件命中数，确认无误后 `--apply` 写回。
- 扫描范围：`Jinshuiyao_Fixed` 下 `.html` / `.css` / `.py`，按 SKIP 列表排除合法/归档路径。

### T04 门禁策略（防复发关键）
在 `tools/closeout_gate.py` 的 `main()` 增加第 5 项 **`禁用色合规`**：
- 扫描 `BASE_DIR` 下 `.html` / `.css` / `.py`
- 排除：`agent_theme.py` / `knowledge/` / `log/` / `docs/旧版资料` / `backups/` / `_old_backups` / `node_modules` / `.workbuddy` / `.git` / `mirofish_db`
- 命中任一处禁止色 → 该门禁 FAIL，列出 `file:line`
- **覆盖 `.py`**：杜绝 653/691/725 类"生成器模板正文漏改"再次漏网

### 防复发闭环
门禁（T04）+ 全量清零（T01-T03）形成"改完即被门禁守住"的闭环，下次任何 AI 或生成器再写禁止色，收工门禁直接拦下。
