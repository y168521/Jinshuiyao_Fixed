# 刀⑤ · SSRF 校验单一真源统一（JS-20260807-01）

> 一句话结论：**服务器判断"一个网址安不安全、能不能去抓取"的规则，现在散落在 2 处几乎一模一样的代码里。本次把它抽成唯一一份（单一真源），两处都来调用它。规则本身一个字都不改，只是搬个家，从此不会再出现"一处修了、另一处忘了修"的隐患。**

---

## 一、道衍推导

- **问阴阳（为何做 / 不做会怎样）**：阳面是"安全规则只有一份，永远一致"；阴面是现在 2 处各写一份，将来任何一处加新判断（比如新增一类内网地址），另一处必漏 → 安全出现缝隙。
- **问三才**：
  - 天（规划）：用户一贯要求"完美前瞻性布局，绝不滞后"——重复实现是典型"将来必踩"的雷，现在排掉。
  - 地（隔离）：核心逻辑下沉到 `core/security.py`（底层），`server/router.py` 与 `core/video_extractor.py`（上层）都来调它，方向 server→core 正常，**无循环依赖**。
  - 人（复盘）：现有测试已覆盖两处旧实现，改完跑测试即可验伤，回归可控。
- **问知止（边界）**：只统一"判断逻辑"，**不动行为**——router 仍返回 `(是否安全, 原因)`，video_extractor 仍返回 `是否安全`；不引入新功能、不扩大改动面。

---

## 二、现状核实（动手前先证伪，不盲信）

### 2.1 两处实现逐行一致（核心证据）

| 判断项 | `router._is_safe_http_url`（server/router.py:773） | `video_extractor._is_safe_host`（core/video_extractor.py:211） |
|--------|------|------|
| 仅允许 http/https | ✅ scheme not in (http,https) → 拒 | ✅ 同 |
| 解析 host | ✅ `socket.getaddrinfo` | ✅ 同 |
| 拒绝 环回/私网/链路本地/保留/组播 | ✅ 五类 `is_*` 全判 | ✅ 五类 `is_*` 全判 |
| 解析失败处理 | 返回 `(False, "域名解析失败")` | 吞异常返回 `False` |
| 返回类型 | `(bool, str)` 元组 | `bool` |

**结论**：IP 危险判定逻辑 100% 相同，唯一差异是返回形态（元组 vs 布尔）和错误原因是否有文案。属于"复制粘贴式重复"，正是漂移温床。

### 2.2 已排除的"想当然"陷阱（与刀③/④ 同一课）

- **`server/handlers/keys.py` 的 `_http_test` 不是 SSRF 残差**：其 `test_url` 来自 `KEY_SLOTS`（keys.py:30-100），**全部硬编码为官方 API 域名**（api.deepseek.com / api.siliconflow.cn / dashscope.aliyuncs.com 等），用户无法改成内网地址 → 无 SSRF 风险，本次**不动**。
- **用户可控 URL → 服务器代取 的入口已全部防护**：`ai.py:254`、`knowledge.py:419/486`、`video_extractor` 入口+重定向链（刀③）均已过校验。SSRF 这块已查完，无新高风险残差，故本次只做"去重"而非"补漏新点"。

### 2.3 无循环依赖（架构核实）

- 新建 `core/security.py` 只 import 标准库 `socket`/`ipaddress`/`urllib.parse`，**不依赖 server 包**。
- `server/router.py` import `core.security`（server→core，正常方向）；`core/video_extractor.py` import 同包 `core.security`。均无反向依赖，不构成环。

### 2.4 编号占用核实

- 全仓搜索 `JS-20260807-`：**无任何占用**（opencode 今日亦未使用），故本刀用 `JS-20260807-01`，不撞号。

---

## 三、设计

### 3.1 新增单一真源 `core/security.py`

```python
# -*- coding: utf-8 -*-
"""金水谣 · 网络安全基础校验（单一真源）

SSRF 防护的核心判定：一个外部 URL 是否允许被服务器代取。
此前该逻辑在 server/router.py 与 core/video_extractor.py 各写一份（近乎复制），
本次下沉为唯一实现，杜绝两处漂移（JS-20260807-01）。
"""
import socket
import ipaddress
import urllib.parse


def is_safe_http_url(url):
    """校验 URL 是否允许被服务器代取。

    仅允许 http/https，且解析后的任一 IP 不得为
    环回/私网/链路本地/保留/组播地址（防 SSRF 访问内网或云元数据 169.254.169.254）。

    Returns:
        (bool, str)：是否安全 + 失败原因（安全时原因为空串）。
    """
    try:
        p = urllib.parse.urlparse(url)
        if p.scheme not in ('http', 'https'):
            return False, "仅支持 http/https 链接"
        host = (p.hostname or '').strip().lower()
        if not host:
            return False, "无效的域名"
        try:
            infos = socket.getaddrinfo(host, None)
        except Exception:
            return False, "域名解析失败"
        for info in infos:
            ip = info[4][0]
            try:
                net = ipaddress.ip_address(ip)
            except Exception:
                return False, "无法解析的地址"
            if net.is_loopback or net.is_private or net.is_link_local \
                    or net.is_reserved or net.is_multicast:
                return False, "禁止访问内网/保留地址"
        return True, ""
    except Exception as e:
        return False, "URL 校验异常：" + str(e)
```

### 3.2 两处委托（行为不变）

**`server/router.py:773`** —— 保持元组返回：

```python
from core.security import is_safe_http_url   # 顶部新增 import

def _is_safe_http_url(self, url):
    # JS-20260807-01：委托 core/security.is_safe_http_url 单一真源，行为不变
    return is_safe_http_url(url)
```

**`core/video_extractor.py:211`** —— 保持布尔返回：

```python
from core.security import is_safe_http_url   # 顶部新增 import

def _is_safe_host(self, url: str) -> bool:
    # JS-20260807-01：委托单一真源，取布尔部分；行为与原实现逐行一致
    ok, _ = is_safe_http_url(url)
    return ok
```

### 3.3 调用方零改动

- `router._is_safe_http_url` 的调用方 `ai.py:254` / `knowledge.py:419,486`（`ok, msg = handler._is_safe_http_url(url)`）拿到的元组与原先一致 → 不受影响。
- `video_extractor._is_safe_host` 的调用方（:241/:270/:290）及既有测试（`test_cors_ssrf_hardening.py:75-82` 的 `is True/False` 断言）拿到的布尔与原先一致 → 不受影响。

---

## 四、为什么低风险（控制回归）

1. **逻辑逐行一致**：新函数直接移植 router 现有实现（含"域名解析失败"等具体原因），video 的旧实现 IP 判定部分完全相同。
2. **接口不变**：router 仍返元组、video 仍返布尔，调用方与测试无需改。
3. **测试护航**：既有 `test_cors_ssrf_hardening.py` 覆盖两处旧接口；本次新增 `tests/test_security_ssrf.py` 直接覆盖新函数。
4. **无循环依赖**：已核实（2.3）。
5. **可本地 import 冒烟**：改完用 `python -c "import server.router, core.video_extractor"` 验证不报循环/导入错。

---

## 五、回滚预案

- 若运行时出现导入异常或行为异常：
  1. `git revert` 本提交即可完全回到两处独立实现状态（本次未删除任何旧逻辑语义，仅改为委托）。
  2. 或直接还原 `router.py`/`video_extractor.py` 两处 `_is_safe_http_url`/`_is_safe_host` 体为原实现（已从 git 历史可取）。
- 单一真源 `core/security.py` 为纯新增、无副作用，即便保留也不影响回滚。

---

## 六、验收标准

- [ ] `core/security.py` 存在，`is_safe_http_url` 对以下全部判定正确：
  - 合法公网 `https://example.com` → `(True, "")`
  - 环回 `http://127.0.0.1` → `(False, ...)`
  - 私网 `http://192.168.1.1` → `(False, ...)`
  - 链路本地 `http://169.254.169.254/latest/meta-data/` → `(False, ...)`（云元数据，SSRF 经典目标）
  - 保留/组播、非 http/https（`file:///etc/passwd`、`ftp://x`）、无效域名、解析失败 → 均 `(False, ...)`
- [ ] `router._is_safe_http_url(url)` 返回值与原实现逐字节一致（既有测试全过）。
- [ ] `video_extractor._is_safe_host(url)` 返回布尔与原实现一致（既有测试全过）。
- [ ] `python -c "import server.router, core.video_extractor"` 无导入错误。
- [ ] `check_consistency` 8/8、`closeout_gate` 5/5 全绿。
- [ ] 四件套登记 `JS-20260807-01`，root↔repo 对齐。
