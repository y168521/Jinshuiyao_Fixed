# ⚠️ 已归档：内容已合并至 `金水谣_契.md` §三（编码规范）。本文件保留供旧链接跳转，新内容以契为准。
# 金水谣 · 编码规范（约束所有模型，避免"缝合怪"）

> 来源：全网「多模型协作代码一致性」方法论（SegmentFault：用规范文件约束所有模型；IBM：命名/异常/注释密度三维度起步）+ 本项目实战踩坑。
> 目的：Qoder / WorkBuddy / 豆包 等不同模型生成的代码，风格与边界**高度一致**，合并后无需逐行改风格。
> 生效方式：任何 AI 改代码前先加载本文件 + 金水谣_模块契约.md。

---

## 1. 命名约定（必须，无"自行判断"空间）

- 函数 / 变量：`snake_case`（如 `calc_kill_rate`）
- 类：`PascalCase`（如 `JinshuiyaoScheduler`）
- 常量：`UPPER_SNAKE`（如 `MAX_BACKUPS`）
- **禁止**：中英混写类名（曾漏 `TianshuPlugin`/`TianshuScheduler` 没改成金水谣）；拼音缩写当变量（除非已是项目约定如 `kb`=知识库）。
- 文件名：模块 `snake_case.py`；测试 `test_*.py` 或 `tests/*/test_*.py`。

✅ 正例：`def extract_video_text(url):`
❌ 反例：`def getTxt(u):` / `class tianshuPlugin:`

## 2. 异常处理模式（统一封装 + 日志）

- 关键路径必须 `try/except`，禁止裸 `except:` 吞错；捕获后 `log` 中文上下文。
- 启动类入口（launch / server）异常**不得静默退出**，要打印原因。
- 外部调用（HTTP/文件）用超时 + 熔断（已有 `core/utils.py` 熔断封装，复用勿重写）。

✅ 正例：
```python
try:
    resp = requests.get(url, timeout=10)
except requests.Timeout:
    log(f"拉取 {url} 超时")
```
❌ 反例：`except: pass`

## 3. 注释密度

- 公开函数 / 类：必须有 docstring（参数 + 返回 + 职责一句话）。
- 内部逻辑：只在"为什么"难懂处注释，不写"做什么"的废话注释。
- 中文注释为主（项目全中文语境）。

## 4. 前端 SPA（workbench.html）特规

- 改完必须 `node --check` 提取出的内联 `<script>`——**一处语法错整页 JS 崩溃**（曾因此整页交互失效）。
- 插入新标签/功能：用**唯一锚点**（nav-item / switchView 映射 / view 区块 / CSS / JS 函数 / 搜索卡 / init 路由），每处断言只命中一次，避免破坏单页结构。

## 5. 依赖与路径

- 第三方依赖只进 `requirements.txt`；启动器（`launch.bat` + `launch_jinshuiyao.py`）首次自动建 venv 并装。
- **禁止写死解释器路径**（旧 `D:\python38`、`E:\下载` 都踩过坑）→ 用 `py` 启动器 + `%LOCALAPPDATA%` 自动 venv。
- 密钥只放 `~/.jinshuiyao-secrets/`，绝不进坚果云同步。

## 6. 大文件 Edit 规范（本环境实战）

- 超大 HTML/MD 用 Edit 常报"未读"——改用 **Python 补丁脚本**（精确字符串替换 + `count==1` 断言）更稳。
- 任何批量改名：utf-8/gbk 自适应；先 dry-run 统计命中数，再执行。

## 7. 测试隔离规范（防测试债复发 · 2026-07-22 立）

> 金水谣曾背 9 项"偶发失败"测试债，根因**全是测试未隔离**（非生产 bug）。本节约防止复发。

- **禁止读写项目内持久共享目录**：测试不得依赖 `金水谣数据/`、`金水谣数据/test_creator_*`、各 `users/` 等业务目录。它们跨运行残留脏数据，且 Windows 文件锁让 `tearDown` 的 `rmtree` 偶发失败 → 空断言误判（典型症状：**单独跑通过、全量跑偶发失败**）。
- **统一用独立临时目录**：`setUp` 一律 `tempfile.mkdtemp()`（或 pytest 内置 `tmp_path`），`tearDown` 清理该临时根；任何"无需持久"的测试都不碰项目目录。
- **被测类若硬编码持久根 → 优先加可选 `root_dir` 参数**（默认行为不变），让测试注入隔离目录；**不要**靠在测试里 `rmtree` 持久目录来"清理"（Windows 锁下不可靠）。
- **禁止用 `@skip` / 注释掉失败测试来"清零"**：必须真修隔离（参考 `模型/金水谣_测试债清理追踪.md`）。
- **新增/修改涉及文件 IO 的被测类，先问"它的数据根能注入吗？"**——可注入性即可测试性。
- **铁律**：任何 AI 说"测试全绿"前，必跑 `py -3.14 -m pytest tests/ -q`（非只跑单个文件），并贴输出摘要。
