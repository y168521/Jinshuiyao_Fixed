# ⚠️ 已归档：内容已合并至 `金水谣_契.md` §五（多机部署）。本文件保留供旧链接跳转，新内容以契为准。
# 金水谣 · 多机部署与同步边界说明

> 目的：台式电脑（已部署，D 盘）与笔记本电脑（待部署，E 盘）两套环境，避免「源码同步了、但本地 venv / 配置悄悄分叉」。
> 维护：JS-20260728-04 环境迁移后补充，条目 JS-20260728-05（2026-07-28 据「笔记本在 E 盘」修正盘符逻辑）。
> 读者：小白也看得懂——照第四节步骤在笔记本做一遍即可。

## 一、一句话结论
**源码走坚果云自动同步；虚拟环境（venv）是每台机器本地自己建的，不会自动同步。** venv 统一放 `<本机盘符>:\Project_Env\jinshuiyao_env`，**盘符随机器不同**（台式 D: / 笔记本 E:），`launch.bat` 策略0 自动扫描所有盘符，所以双机源码保持单一、不用改码。

## 二、两台机器角色（固定，不分「本机」）
| 机器 | 角色 | venv 路径 | Python |
|------|------|-----------|--------|
| 台式电脑 | ✅ 已部署（2026-07-28 完成迁移） | `D:\Project_Env\jinshuiyao_env`（61 包，含 scikit-learn） | 3.14.6 |
| 笔记本电脑 | ✅ 已部署（2026-07-28 完成，JS-20260728-09） | `E:\Project_Env\jinshuiyao_env`（61 包，含 scikit-learn） | 3.14.6 |

> 盘符差异是关键：笔记本没有 D 盘，所以 venv 放 E 盘；`launch.bat` 会自动找，你不用改任何代码。

## 三、同步边界（什么走云、什么本地）
**走坚果云自动同步（双机应一致）：**
- 源码 `Jinshuiyao_Fixed/`（含 `launch.bat`、`requirements.txt`、`engines/`、`server/`、`jinshuiyao-guide/` 等）
- 留痕文档（`工作留痕总索引.md`、`AI协作交接中心.md`、`.workbuddy/memory/`）
- `requirements.txt`（锁版本，双机装同一组 61 个包）

**每台机器本地（不同步、各自维护）：**
- 虚拟环境 venv（`<盘符>:\Project_Env\jinshuiyao_env`，~600MB，**不该进云**）
- 运行日志 `C:\Users\Administrator\AppData\Local\Jinshuiyao\`
- 金水谣数据（`金水谣数据/` 在云盘内会同步——注意这是「数据」不是「环境」）

> ⚠️ 关键：坚果云只同步「文件」，**不会**帮你建 venv。笔记本拿到同步后的源码后，必须自己 `python -m venv` 建环境并 `pip install -r requirements.txt`。

## 四、venv 路径约定（防不同步的核心）
双机 venv 都放 `<本机盘符>:\Project_Env\jinshuiyao_env`，**盘符随机器**（台式 D: / 笔记本 E:）：
- `launch.bat` 的「策略0」已改为**自动扫描 C/D/E/F/G/H 盘符**找 `Project_Env\jinshuiyao_env\Scripts\python.exe`，命中即用。
- 所以台式放 D:、笔记本放 E:，launch.bat **无需任何改动**，双机源码保持单一、不分歧。
- 若某机想放其他自定义路径（非 `<盘符>:\Project_Env\jinshuiyao_env`），才需要改 `launch.bat` 策略0。

## 五、笔记本部署步骤（照做即可 · 笔记本在 E 盘）
1. 笔记本登录同一坚果云账号，等 `模型/` 文件夹同步完成（含 `Jinshuiyao_Fixed/`）。
2. 确认 Python 3.14.x 已装（`py -3.14` 可用，或记下安装路径；笔记本 Python 可能在 C: 或 E:）。
3. 建 venv（笔记本用 `E:\Project_Env\jinshuiyao_env`；用系统 python 创建以避开中文父目录编码乱码）：
   `E:\下载\python.exe -m venv E:\Project_Env\jinshuiyao_env`
   （若笔记本 Python 不在 `E:\下载`，改用实际路径，例如 `C:\Python314\python.exe`）
4. 装依赖：
   `E:\Project_Env\jinshuiyao_env\Scripts\python.exe -m pip install -r Jinshuiyao_Fixed/requirements.txt`
5. 验证装全：
   `E:\Project_Env\jinshuiyao_env\Scripts\python.exe -c "import requests,numpy,scipy,pandas,akshare,sklearn,matplotlib,customtkinter; print('OK')"`
6. 启动：双击 `启动金水谣助手.bat`（自动扫描盘符命中 E: venv）→ 开 `http://localhost:18888/math-model.html` 验证。
7. 健康检查：`curl http://localhost:18888/health` 应 200；`/api/lottery/math-model?lot=大乐透` 返回 `ok=true`、注单数≈149、增益≈0。

## 六、防不同步 Checklist（每次换机器前核对）
- [ ] 源码是否已同步（坚果云同步完成 / `git status` 干净）
- [ ] 本机 venv 是否存在且含 sklearn（第 5 步 import 测试通过）
- [ ] 若改过 `requirements.txt`，**另一台必须重跑** `pip install -r`
- [ ] 盘符不同没关系——只要都叫 `<盘符>:\Project_Env\jinshuiyao_env`，launch.bat 自动扫描
- [ ] 活服务重启用本机 venv（双击 `.bat` 即可，自动命中）

## 七、已知坑（两台机器都一样）
- **中文路径建 venv 编码乱码**：用系统 python（如 `E:\下载\python.exe`）创建，或放英文父目录。
- **旧进程占用 18888**：重启前 `taskkill /F /PID <pid>`；若「拒绝访问」需在提权窗口执行。
- **server 运行时不需要 sklearn**（仅 IDE/独立脚本需要），故即使退化为 system python，服务功能无损。
- **要求精度一致**：双机都用 Python 3.14.x，避免小版本差异导致包行为不同。
- **盘符不同是正常**：台式 D:、笔记本 E:，venv 名字 `Project_Env\jinshuiyao_env` 一致即可，launch.bat 扫全盘符。
