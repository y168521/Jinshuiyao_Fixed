# 金水谣 · 标准唯一真源（What IS authoritative）

> **本文件是"标准层"的唯一注册表，不是又一份标准。**
> 它只回答三个问题：**这条规则谁说了算（唯一真源在哪）**、**哪些地方只是引用（禁止改）**、**改了之后要同步哪里**。
> 创立原因：同一份规范在 `Jinshuiyao_Fixed\`（真源）、`模型\`（根镜像）、`obsidian-vault\`（导出）、`90_归档文档\`（归档副本）四处共存，
> 且**数值型阈值同时写在代码常量和若干份 md 里**，谁改一处不改另一处，就会形成 A/B 两套口径——这正是"每个 AI 各有各的标准"的物理根因。
> 创立：2026-09-20 · 关联 JS-20260920-10 · 维护责任人：任何改了下面表格里任一数值的 AI（改者即为本次维护人，必须同步本表）

---

## 一、副本拓扑（单向，禁止反向写）

| 层级 | 位置 | 角色 | 谁能写 |
|---|---|---|---|
| ① 真源 | `Jinshuiyao_Fixed\<文件>` | **唯一可编辑**，进 git | 人 + AI（唯一入口） |
| ② 根镜像 | `模型\<同名文件>` | `check_consistency.py` GITSYNC 门禁要读；**只读副本** | 只由 ① 单向 `Copy-Item` 覆盖，且必须把 mtime 刷到 ≥ ① |
| ③ 导出 | `obsidian-vault\<同名文件>` | Obsidian 双链浏览用，**只读产物**（末尾带 `📎 关联文档` 页脚） | 只由同步脚本从 ① 生成，**人工/AI 都不要直接编辑** |
| ④ 归档 | `90_归档文档\<文件>` | 历史快照，冻结。文件首行必须自述"已归档、以 X 为准" | 冻结，只读；更新内容写到 ① |

**铁律**：任何 AI 改留痕/规范时，**先确认自己在 ① 里**。写完执行：
`改 ① → Copy-Item 覆盖 ②（mtime 刷到 ≥ ①）→ 跑 check_consistency.py PASS → 提交`。
③ 由既有同步链路自行刷新，**不要手写**。

> 已知待处理：`90_归档文档\金水谣_全视角审查清单.md` 与仓库版同名共存（当前字节相同，但没有任何单向同步保证）→ 列入本节下方"待办"。

---

## 二、权威矩阵（规则 → 唯一真源 → 只读引用）

| # | 规则域 | **唯一真源（改这里）** | 只读引用（**禁止在这里重写规则**） |
|---|---|---|---|
| 1 | 视觉/配色（L2 owner 默认主题、层级） | `金水谣_契.md` §五 | `默认WebUI风格.html`、各 HTML 页、`agent_theme.py` |
| 2 | 铁律 + Pipeline（做什么之前必做） | `金水谣_纲.md` | `AGENTS.md` 开头 "必读顺序" |
| 3 | 协作流程 / 防乱机制 / 防惰五道防线 | `金水谣_录.md` | `AI协作交接中心.md` §六 系列、`.workbuddy/skills/*` |
| 4 | **数值型阈值**（见 §三） | **代码里的常量**（不是任何 md） | 本表 §三、`MEMORY.md`、经验收集箱条目 |
| 5 | 编码风格 / 模块边界 / 多机同步 | `金水谣_编码规范.md`、`金水谣_模块契约.md`、`金水谣_多机部署与同步边界.md` | 各 Skill 的示例段落 |
| 6 | 提交闸门（4 闸） | `tools/pre-commit-hook-wrapper.sh`（**真源**，`install_hooks.py` 复制到 `.git/hooks/`） | 手改 `.git/hooks/pre-commit` 会被覆盖，白改 |
| 7 | 收工门禁（8 闸）与本轮事项 | `tools/closeout_gate.py` + `tools/item_register.py` | `AI协作交接中心.md`、`工作留痕总索引.md` |
| 8 | 留痕编号（JS-YYYYMMDD-NN）与三处同号 | `AI协作交接中心.md` + `工作留痕总索引.md` + `金水谣数据/log/经验收集箱.md`（**三份同时写，缺一不算登记**） | `金水谣数据/log/ai_decisions.md` 决策卡（引用编号，不生成编号） |
| 9 | 高频错误的"防再犯清单" | `tools/ai_guard_rules.md` | `AGENTS.md` 铁律 -1 摘要 |
| 10 | 机器可执行的数据源/ Env 细节 | `.workbuddy/skills/<name>/SKILL.md`（每个 Skill 自查是否覆盖上述同一条规则，有重叠即视为过时） | — |

**冲突裁决优先级**（出现矛盾时按序采信）：
`① 代码常量/脚本实际行为` ＞ `② 本文件的 §三 阈值表` ＞ `③ 契/纲/录` ＞ `④ 交接中心/总索引/经验箱` ＞ `⑤ MEMORY.md` ＞ `⑥ Skill 描述文`。
**注意层级 ① 最高**：代码跑出来的行为才是真标准——文档说 30% 而代码写 −30.0，以代码为准，并立刻回来修本表。

---

## 三、数值型阈值总表（唯一的口径清单）

> 用法：**改阈值 = 改代码常量 → 回来同步本行 → 同步受影响的测试/留痕 → `check_consistency.py` PASS**。
> 任何 md 里出现的同名阈值，一律视为本表的"引用副本"，不得与下表不一致。

### 3.1 基金外围风险 ——`domains/fund/fund_profile_risk.py`

| 阈值含义 | 常量名 | 当前值 | 生效于 |
|---|---|---|---|
| 清盘线（连续 60 工作日低于可终止） | `SCALE_DANGER_YI` | 0.5 亿 → danger | `eval_scale_risk` |
| 迷你基金线 | `SCALE_WARN_YI` | 2.0 亿 → warn | `eval_scale_risk` |
| 规模单季**跌幅**预警 | `SCALE_DROP_WARN_PCT` | −30.0% → 升级档位 | `eval_scale_risk` |
| 规模单季**涨幅**预警（暴增） | `SCALE_SURGE_WARN_PCT` | +100.0% → 升 warn（JS-20260920-08 增补，原函数只判跌） | `eval_scale_risk` |
| 经理变更预警窗口 | `MANAGER_CHANGE_WARN_DAYS` | 180 天 → warn（JS-20260920-15 从函数默认参数提出为常量，已纳入 §五-3 机器闸门） | `eval_manager_change` |
| 配置经理名与线上不符 | — | **notice**（不是新发变更，禁止升 warn 淹没真变更） | `eval_manager_change` |
| 计划日投（限购比较基准） | `plan_investment_monthly ÷ 30` | 各基金 `investment` 配置 ÷ 30 | `eval_purchase_limit` |
| "无单日限额"的表示 | `None` | **绝不用 0**（0 会被判成最低档假阳性） | `parse_purchase_limit` |
| 缓存 TTL | `MANAGER_TTL_HOURS=24`、`SCALE_TTL_DAYS=7`、`PURCHASE_TTL_HOURS=24` | — | 三个 fetcher |

### 3.2 数据真实性守卫 ——`core/data_truth_guard.py`

| 阈值含义 | 常量名 / 值 | 说明 |
|---|---|---|
| 历史赛果新鲜度 warn | `MATCH_STALE_WARN_DAYS` = 30 天 → warn | 读 `matches_real.csv`，**不要改成用文件 mtime 判**（JS-20260917-04 专门修过） |
| 历史赛果新鲜度 fail | `MATCH_STALE_FAIL_DAYS` = 180 天 → fail（赛季彻底过时） | 同上；JS-20260920-15 由函数体魔数提出为常量，已纳入 §五-3 机器闸门 |
| 彩票数据新鲜度 | `LOT_STALE_WARN_DAYS` = 3 天 → warn | 按 `金水谣数据/lottery/` 下各彩种 JSON 的 mtime 判；JS-20260920-15 提出为常量 |
| 赔率合理性 | 仅越界（<1.01 或 >1000）才告警；空串/None/−1/非正数 = **无该盘口，跳过** | 用 `float()` 判空串会误报（JS-20260920-01 修） |
| 来源分布 | 用 `counts_as_source` 区分「数据型 / 元检测项」，只聚合前者 | 否则检测项自身 source 标签会被当数据来源累加 |

### 3.3 代码体检门禁 ——并入 `closeout_gate.py`

| 指标 | 阈值 | 现状（2026-09-20 实测） |
|---|---|---|
| 最大函数行数 | ≤50 | ✗ 248 文件超阈值（最大 `gui/main_window.py:App` 1863 行）→ WARN 不阻断，**挂账** |
| `Any` 数量 | 0 | ✗ 122 |
| SQL 注入 | 0 | ✓ |
| 明文密钥 | 0 | ✗ 4 → **已定性（2026-09-20）：全部是检测器误报，真密钥 0 条**。四处分别是 `scripts/preflight_check.py` 的 `_PASS="\033[32m[PASS]\033[0m"`、`tools/smoke_test.py` 与 `tools/wrapup/base.py` 的 `PASS_ICON="[OK]"`、`smart-coder/recommender.py` 测试夹具里的样例串 `"API_KEY = 'sk-123'"`。**读报告时直接扣掉这 4**（台账仍按 4 计，因为检测规则没改——见 §五-4） |
| 未超时 requests | 0 | ✓ |
| 数据盲区（`STRONG_DIRS` 缺失数） | 0 | ✓ **已补齐（2026-09-20）**：建 `金水谣数据/music`、`金水谣数据/creator_output`（各带 `.gitkeep`），`closeout_gate` 的"数据完整性"由 ❌ 转 OK |

### 3.4 足球/彩票通用

| 事项 | 口径 | 出处 |
|---|---|---|
| 足彩 DNS 自愈 | `ipconfig /flushdns` 重试 + 双源失败时回退本地缓存（不抛异常、不刷错误日志） | `domains/football/fetcher.py`（与 `fetchers/fetcher.py` 同款） |
| 彩票（3D / 排三 / 大盘彩） | 无预测力，只优化诚实度；`|gain| < 0.02` 判噪声；3D/排三按逗号分组；输出必须带"候选集·非购买建议" | `domains/lottery/*` |

---

## 四、改一条标准的最小闭环（照抄）

1. 在 **① 真源**改（代码常量改在代码里，规则改在契/纲/录里）。
2. 同步 **② 本节 §三 表格**（若改的是数值）与所有"引用副本"（`MEMORY.md`、相关 Skill、受影响的 md）。
3. **`Copy-Item` 覆盖②根镜像并把 mtime 刷到 ≥ ①** → 跑 `check_consistency.py` 确认 PASS。
4. 受影响的值/threshold 若被单测覆盖，必须同步改测试断言（例：`SCALE_SURGE_WARN_PCT` 有 `TestScaleSurge` 守着）。
5. `item_register.py add → … → close <id> JS-编号`，三处留痕同号，`closeout_gate.py` RC=0 才可收工。

---

## 五、本节下方待办（下次动这套文档体系时优先清）

1. **【已完成 2026-09-20·JS-20260920-15】提常量**：`MANAGER_CHANGE_WARN_DAYS = 180` 已从 `eval_manager_change` 的默认参数提出为模块级常量（函数签名改为 `recent_days: int = MANAGER_CHANGE_WARN_DAYS`，行为不变），并纳入 §五-3 机器闸门。教训记一笔：**阈值写死在函数签名里，等于没有常量**——文档无法指向它，改的人也找不到它。
2. **收副本**（历史遗留的"同名散装副本"，都是潜在分叉源，处置方式：**能收敛的收敛、不能删的至少刷新并标注**）：
   - `模型\经验收集箱.md`（根目录陈年副本，2026-09-02 就落后 69 行被记过一笔）→ 已刷到与 `金水谣数据\log\经验收集箱.md` 一致，**建议后续退役**（在 `.gitignore` 或同步脚本里排除）。
   - `90_归档文档\金水谣_全视角审查清单.md` 与仓库版同名共存且无单向同步 → 要么删除归档副本（首行已声明归档的照此办理），要么在同步脚本里加"①→④"覆盖步骤。
3. **【已完成 2026-09-20·JS-20260920-14】闸门已上线**：`tools/check_consistency.py` 新增检查项 **`标准阈值-代码常量`**，用 AST 提取 `domains/fund/fund_profile_risk.py` 的模块级数值常量，校验**仓库真源 + 根镜像**两份阈值表是否都登记了同一数值；不一致即 FAIL（裁决方向：**以代码为准，回来改文档**）。异常一律降级跳过，绝不因闸门自身 bug 阻断提交。
   - **上线即踩到两个"闸门自身的假绿"，都已修**（值得记住：新闸门必须先自测能不能抓到错误，否则等于没装）：
     ① 只判 `ast.Assign` → 带注解的 `X: float = 1.0`（`AnnAssign`）被漏；
     ② 只判 `ast.Constant` → **负数**在 AST 里是 `UnaryOp(USub, Constant(30.0))`，导致 `SCALE_DROP_WARN_PCT = -30.0` 整条漏检。
   - 覆盖范围目前是**基金外围风险 7 个常量**（`SCALE_DANGER_YI/WARN_YI/DROP_WARN_PCT/SURGE_WARN_PCT`、`MANAGER_/SCALE_/PURCHASE_ TTL`）；`data_truth_guard`、`code_health_gate` 的阈值待后续按同样方式纳入。
4. ~~**明文密钥 4 处**：逐条确认是真泄露还是样例误报~~ → **已定性：4 处全是误报**（结论见 §3.3）。**是否改检测规则由人拍板**，AI 不得擅自放宽密钥检测——**漏检真密钥的代价远高于误报**。候选规则（供人工确认）：只判赋值右侧的**高熵字符串**，并排除标识符名含 `PASS`/`ICON` 的常量与测试夹具目录。
5. **代码体检的历史包袱**（248 文件超函数行数、最大 `gui/main_window.py:App` 1863 行、Any 122）：按**"新增代码零增量、存量按年收敛"**处理，禁止一次性重写。建议只做两件事：①给 `code_health_gate.py` 加"基线快照"——只告警**新增**的超阈值文件（防止继续变坏）；②把 `gui/main_window.py:App` 单列为一个长期重构任务。
6. **【已完成 2026-09-20·JS-20260920-13】L3 技能层已接进本表**（原状态：2026-09-20 上午实测）：`.workbuddy/skills`（15 个）+ `.opencode/skills`（22 个）共 **37 个 SKILL.md，引用本文件的 = 0 个**。这意味着**任何按技能执行的 AI 根本看不到这份标准**——§二 的权威矩阵只在"人主动翻文档"时才生效，而实际执行路径是技能。**这是"统一标准"目前最大的落地缺口。** 修法（两步，低风险）：
   - ①给 15 个 `jinshuiyao-*` 技能各加一句"开工先读 `金水谣_标准唯一真源.md` §二/§三"；
   - ②顺手清过期项：7 个技能仍写 `py -3.14`/`py -3`（**本机没有 py 启动器**，须写全路径 `E:\Project_Env\jinshuiyao_env\Scripts\python.exe`）——`jinshuiyao-ai-decisions-check`、`jinshuiyao-frontend-audit`、`jinshuiyao-frontend-health-probe`、`jinshuiyao-lottery-honest-backtest`、`jinshuiyao-safe-cleanup`、`jinshuiyao-service-watchdog`、`.opencode/skills/jinshuiyao-docs`。
   - ③重叠待判：`jinshuiyao-frontend-health-probe` 自述"复用 `jinshuiyao-frontend-audit` 方法" → 二者高度重叠，考虑合并或明确分工。
   - **执行结果**：20 个 `jinshuiyao-*` 技能（`.workbuddy/skills` 15 + `.opencode/skills` 5）**全部注入引用块**（幂等，含"只能改 `Jinshuiyao_Fixed\` 真源"提示）；`py -3.14` 残留 **7 → 0**；frontend 重叠判定为**分工互补不合并**（audit=定位型 / health-probe=巡检型），并在 health-probe 里写明"改探测方法时两个技能必须同步改"。**未动的 17 个通用技能**（`api-design`、`security-best-practices` 等）与项目标准无关，保持原样。
7. **未启用功能目录的定性**：`music`、`creator_output` 目前是空目录（`core/dispatch_music.py` 会提示用户往里放音频），属于"设计上必需、业务上未启用"。若长期无内容，应从 `STRONG_DIRS` 降为 OPTIONAL，或保持 `.gitkeep` 现状——**两者都行，但不能让它重新变回一个假红**。
