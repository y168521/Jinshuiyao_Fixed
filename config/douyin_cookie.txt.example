# ⚠️ 抖音登录态 cookie 配置示例（请勿提交到仓库 / 云同步！）
#
# 本文件是「示例」。要启用抖音带登录态提取，请把本文件复制为：
#        config/douyin_cookie.txt
# 并在里面只粘贴你自己的抖音 cookie 字符串（从浏览器开发者工具复制）。
#
# ─────────────────────────────────────────────────────────
# 如何获取你自己的 cookie（手动，绝不自动偷取）：
#   1. 用浏览器登录 douyin.com
#   2. 按 F12 打开「开发者工具」→ Network（网络）标签
#   3. 刷新页面，点任意一个 douyin.com 的请求
#   4. 在请求头（Request Headers）里找到「Cookie:」那一行
#   5. 把整行 cookie 值复制，粘贴到 douyin_cookie.txt
#
# 也可以不建文件，而是：
#   - 运行时传参：ingest_to_kb(url, cookie="你的cookie")
#   - 或设环境变量：set TIANSHU_DOUYIN_COOKIE=你的cookie
#
# 安全提醒：
#   cookie 等同于你的登录凭证。请勿分享给他人，勿提交到 git / 坚果云。
#   建议把 config/douyin_cookie.txt 加入忽略列表。
# ─────────────────────────────────────────────────────────
