# 金水谣 · 录（人层 · 过程记录）

> 本文件是金水谣项目的**协作过程手册**。接手SOP、审查清单、防乱机制、风险登记、失败模式速查都在这里。
> 顶层原则见 `金水谣_纲.md`，执行契约见 `金水谣_契.md`，进度跟踪见 `AI协作交接中心.md`。
> 全文吸收了原 11 份规范文档的过程相关部分 + 经验收集箱高频模式 + 风险登记册。

---

## 一、接手 SOP（新 AI 开工 5 分钟检查清单）

| 步骤 | 动作 | 正常信号 |
|------|------|----------|
| ① | 读 `AI协作交接中心.md` + `金水谣_纲.md` | 知道项目状态和铁律 |
| ② | 读 `金水谣_契.md`（编码规范+模块边界+机制契约） | 知道怎么改代码 |
| ③ | 读本文件（协作规范+防乱+失败模式速查） | 知道怎么做完收工 |
| ④ | 看 `工作留痕总索引.md` 最近3条 | 确认最新状态 |
| ⑤ | 换机器/开工前：`git pull`（或双击 `同步代码.bat`） | 本地是最新代码 |
| ⑥ | 跑 `py -3.14 tools/gate.py --check` | 36项全绿=交接完整 |
| ⑦ | 换机器时额外执行：交接中心环境检查 | 环境就绪 |

## 二、全视角审查清单（开工前扫5行）

| # | 视角 | 最擅长 | 开工前检查 |
|---|------|--------|-----------|
| 1 | **工程实现** | Qoder | 命名合规/调用方同步改完/重复代码消除 |
| 2 | **测试运行** | WorkBuddy | ruff/AST/smoke 过了吗/测试隔离了吗 |
| 3 | **架构安全** | TRAE | 跨模块影响/密钥/SSRF/XSS/fail-closed |
| 4 | **发散替代** | 豆包 | 更简单方案/异常路径/被否决方案记录 |
| 5 | **业务体验** | 用户兜底 | 用起来顺不顺/中文一致/投入产出值不值 |

三层互补：开工前扫本清单 → 改完走 Pipeline(纲§四) → 收工跑 gate。

## 三、标准工作流程

```
Step 0 同步代码：git pull（或双击 同步代码.bat），确保本地是最新代码
Step 1 上下文初始化：读交接中心+纲+契+录+总索引
Step 2 对齐规范：文件注册/路由/PAGE_ROUTES/safe_write_json
Step 3 精准执行：聚焦需求，极简落地，自测通过
Step 4 收尾双归档：更新交接中心 + 经验收集箱
Step 5 收工自检：py -3.14 tools/gate.py --check 全绿
Step 6 同步到远程：git push（或双击 同步代码.bat），确保双机一致
```

## 四、任务认领与防冲突

- **开工前必认领**：在交接中心待优化清单改状态为"进行中·XXX认领"
- 24 小时无进展自动解锁
- 先查 gatedoc 当前任务状态，不撞车

## 五、质量保障

### 改动前影响面分析
```
【改什么】：一句话
【直接文件】：哪些文件
【间接影响】：其他模块/前端/API/数据格式
【风险等级】：低/中/高/极高
【回滚难度】：极易/容易/中等/困难
```

### 改后验收清单
- pytest 相关模块全过（用 venv 下的 python，不要用 `py -3.14`）
- `py -3.14 tools/gate.py --smoke --quick` 7/7
- 高风险改动：实际打开页面验证

### 收工完成定义（DoD · 五项门禁）
**为什么存在**：历史漏项根因不是 AI 偷懒，而是"完成清单本身不完整"——只含登记+commit，不含知识库决策卡、三层闭环。以下五条全部机器可核对：

1. **[A] git 状态**：待提交清单已清，gitignore 无隐患；本地 commit 后已 `git push` 到远程（确保双机一致）
2. **[B] 索引↔知识库覆盖**：今日总索引每个 JS 编号都进了 `ai_decisions.md` 知识库（有对应决策卡）
3. **[C] 决策卡 10 字段完整**：今日每张卡必含以下 10 字段——
   - 属主、做了什么、为什么(根因)、验证、踩过的坑、有效方法、关联文件、关联总索引、反事实对照、置信度
   - 彩票/预测类卡另查反事实三字段：`counterfactual_baseline` / `baseline_comparison` / `honesty_note`
4. **[D] 三层闭环旁证**：若今日改了铁律/方法类文件，启动提示词.txt 必须同步更新
5. **[E] commit↔索引交叉核对**：今日 git commit 信息里的 JS 编号，都必须在总索引 `###` 标题行出现过

以上全部由 `tools/closeout_gate.py` 只读核对，绝不 git add/commit。

## 六、风险登记册（R1-R10）

| 编号 | 风险 | 概率/影响 | 缓解措施 | 监护脚本 |
|------|------|-----------|---------|---------|
| R-001 | 免费模型服务突变（切换/收费/下线/限流） | 高/高 | free_model_pool 五层防护：外部化配置→故障转移→容错降级→健康探针→失败报警 | `scripts/free_model_health_check.py` |
| R-002 | 坚果云同步损坏/丢失文件 | 中/极高 | 三层备份(活/副本/保险) + git 版本化 + 门禁去盲区 | `scripts/jinshuiyao_data_guard.py` |
| R-003 | 多 AI 会话互写冲突 | 高/高 | 会话租约 Lease (acquire/release) + 全局写锁 | `scripts/session_coordinator.py` |
| R-004 | 系统 Python 与 venv 混淆跑测试 | 高/中 | 启动提示词明确警告；launch_jinshuiyao.py 自动建 venv | — |
| R-005 | 删除文件无 salvage 兜底 | 中/极高 | safe_cleanup 七类划分+三级闸门+回收站 | `scripts/safe_cleanup.py` |
| R-006 | 密钥泄露到同步盘/日志 | 高/极高 | 密钥隔离到 ~/.jinshuiyao-secrets/，代码内无落盘 | `tools/set_secret.py` |
| R-007 | 回测命中率幸存偏差（min_hit=1 假绿） | 高/高 | 三数检验法(去极值净值+随机基线+理论期望) | `scripts/backtest_lottery_honest.py` |
| R-008 | 新 AI 不读文档直接开工 | 高/中 | 开工必读铁律(纲§三-1) + 接手SOP(本文件§一) | — |
| R-009 | gate.py/ops.py 未覆盖脚本被遗忘 | 低/中 | 定期扫描 tools/ + scripts/ 未注册脚本 | — |
| R-010 | 总索引登记作假（编造编号/字段偷懒） | 中/高 | wrapup_check 内容质量校验（文件哈希+改动量） | `tools/wrapup_check.py` |
| R-012 | 双机代码不同步（笔记本改了台式机未pull，或反之） | 中/高 | 开工前 `git pull` + 收工前 `git push` 铁律；`同步代码.bat` 一键双向同步；笔记本设每小时定时任务 | `同步代码.bat` |
| R-011 | LLM 付费调用无成本预算/无重试上限/被恶意刷量导致烧钱或雪崩 | 高/高 | G2成本闸(`allow_paid()`预检+日/分/单笔上限+跳闸冷却1h) + G3限流(每IP令牌桶+全局≥500%突增跳闸) + G8超时+更便宜fallback + G7调用级熔断 | `core/llm_budget.py`/`server/rate_limiter.py`/`core/free_model_pool.py` |

## 七、高频失败模式速查（PAT-001~015）

> 从经验收集箱 + pattern_library.json 提取。每遇相似症状先查此表。

| 编号 | 模式 | 症状 | 根治方案 | 来源 |
|------|------|------|---------|------|
| PAT-001 | 共享容器读-改-写无锁 | 数据项丢失/间歇性 KeyError | `with _LOCK:` 包裹整个读改写；嵌套用 `RLock` | JS-20260722-20 |
| PAT-002 | try 改状态缺 finally | 异常后系统行为异常 | `old=x; try: ...; finally: x=old` | 经验箱 |
| PAT-003 | set/dict 切片越界 | `TypeError: 'set' object not subscriptable` | `sorted(hot_nums)[:keep]` 先转 list | JS-20260723-35 |
| PAT-004 | 伪造数据填充 | 全源失败时返回随机数→命中率虚高 >95% | 失败返回 `ERROR, []`，绝不伪造 | JS-20260723-28 |
| PAT-005 | 裸 except 吞异常 | 无法定位问题/间歇性故障 | `except SpecificError as e: logger.error(...)` | AST-002 |
| PAT-006 | 硬编码密钥/路径 | 换机后报错/密钥泄露 | 密钥走 `get_api_key()` / 路径走 `config/paths.json` | 安全规范 |
| PAT-007 | SSRF 无目标校验 | 安全扫描报 SSRF 风险 | `_is_safe_http_url()` 校验 + 禁内网 | 安全规范 |
| PAT-008 | 旧项目名残留 | import TianShu → 模块找不到 | 全局搜索 `TianShu`/`Tianshuiyao`/`guide_server` | AST-001 |
| PAT-009 | 重复代码块（同逻辑多处复制） | 改一处漏一处 | 抽取为统一函数（如 `_fetch_from_sources`） | JS-20260723-31 |
| PAT-010 | 可变默认参数 | 第二次调用行为异常 | `def f(items=None): if items is None: items = []` | AST-004 |
| PAT-011 | print() 调试残留 | GBK 终端报编码错误 | 改用 `logger.info()` + `_safe_icon()` | AGENTS.md |
| PAT-012 | `import *` 污染命名空间 | NameError 追不到来源 | `from module import specific_name` | AST-011 |
| PAT-013 | GraphRAG sources/triples 不匹配 | 知识图谱统计数据不一致 | `_save_triple_store` 时 `_recompute_sources()` | MEMORY §4 |
| PAT-014 | 误用解释器双进程孤儿 | 两个进程抢 18888 端口 | 必须 `py -3.14` 或双击启动器 | 经验箱 |
| PAT-015 | 回测 hit 失真(min_hit=1) | 命中率 95-100% 明显不可信 | `_evaluate_hit` 直选精确/组选容错双档，min_hit>=3 | JS-20260723-37 |
| PAT-019 | 付费API无成本预算/无上限重试 | 单日账单异常/被刷量烧穿预算 | `allow_paid()`预检+三重上限+廉价fallback+限流 | JS-20260729-10 |
| PAT-020 | 外部调用无超时/无重试上限 | 请求hang死/线程耗尽雪崩 | 严格timeout+重试上限+更便宜fallback+并发门 | JS-20260729-10 |

## 八、防乱机制速查

### 漏洞清单（已修复 30+ 项，持续更新）
| 分类 | 典型漏洞 | 状态 |
|------|---------|------|
| 留痕造假 | 瞎写JS编号/写"已完成"没改代码 | 文件哈希+改动量校验 |
| 字段偷懒 | 必填写"无"凑数 | 内容质量校验 |
| 测试逃避 | 天天--skip-tests | 频率限制连2次强制跑 |
| 安全漏洞 | 密钥裸奔/XSS注入 | SAST 15模式+XSS 6级检测 |
| 配置漂移 | 改A处常量忘改B处 | 配置一致性检查 |
| 成本失控 | 付费API无预算/无限流被刷爆 | G2成本闸+G3限流+遥测审计 |

### 变量管理体系
- 前缀：`STATUS_`/`CONFIG_`/`COUNT_`/`FLAG_`/`OWNER_`/`RISK_`/`QUALITY_`
- 只读变量：严禁修改（CONFIG_PORT 等）
- 受限变量：改必留痕日志
- 自由变量：随便改

### 七步链式验收闭环
接手 → 方案 → 编码 → 测试 → 安全 → 留痕 → 收工
顺序不可跳，红灯必须修，超时必汇报。

### 经典被否决方案索引
| 方案 | 为什么否决 | 替代 |
|------|-----------|------|
| `register(name, func, interval)` 表达"每月1号" | interval 只支持"每 N 分钟"，不支持 cron | 每日触发 + `datetime.now().day != 1` 守卫 |
| `min_hit=1` 回测（3码任中1即算命中） | 随机基线达 95-100%，假绿 | 双档+随机基线+3字段校验 |
| 并发写用 `dict.update()` 无锁 | 多线程写丢失中间状态 | `with _LOCK:` 包裹整个读改写 |
| 删除文件用 `shutil.rmtree()` | 误删不可恢复 | `safe_cleanup.py` 七类+salvage |
| 用 `pip install` 直接改系统 Python | 依赖冲突/权限问题 | venv 隔离 + `requirements.txt`
