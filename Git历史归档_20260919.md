# Git 历史归档 · 2026-09-19 仓库修复

> 本文件记录 2026-09-19 对 `Jinshuiyao_Fixed` 仓库的一次**历史修复**：
> 有 6 个提交的文件快照（git tree 对象）在 D 盘故障中永久丢失，无法重建，
> 因此以最后一个健康提交为父重建了桥接提交，并把被跳过提交的完整说明归档在此。
> 登记：JS-20260919-05 / W63补120。

---

## 一、起因

2026-09-17 固态硬盘（D 盘）故障 + 系统重装。仓库放在坚果云同步盘上，
同步过程对 `.git/objects` 里的**松散对象文件**有丢失（同步盘不适合放 git 仓库）。

用 `git fsck` 扫出**缺 9 个对象**，其中 6 个是提交的「文件快照根树」：

| 丢失对象 | 所属提交 | 后果 |
|---|---|---|
| `84cfadc30f67` | `84cfadc` | 该提交无法 `git show` / `git diff` |
| `78be3f13343b` | `78be3f1` | 该提交无法 `git show` / `git diff` |
| `dbced5e9e4c8` | `dbced5e` | 该提交无法 `git show` / `git diff` |
| `ace61e5efbc1` | `ace61e5` | 该提交无法 `git show` / `git diff` |
| `9c928bed47d1` | `9c928be` | 该提交无法 `git show` / `git diff` |
| `3968829b1f9b` | `3968829` | 该提交无法 `git show` / `git diff` |

后果：`git show`/`git diff` 走到这些提交就 fatal，`git gc` 卡死失败，
`git push` 打包时也会因为遍历到缺失对象而失败 —— **这就是同步任务一直报错的原因之一**。

---

## 二、修复方式（内容零丢失）

关键事实：**git 的每次提交都保存一份全量文件快照（tree）**。
所以那 6 个提交承载的所有文件内容，都已经包含在它们之后的提交里 —— 
丢的只是「那几个时间点的快照」本身，不是文件内容。

修复动作：

1. 逐个校验每个提交的树是否完整，找到**最靠后的、且祖先链全部健康**的提交：
   `1e4f2827`（2026-08-24「模式库0字节事故根治」），其祖先链 3387 个对象、**0 缺失**。
2. 以 `1e4f2827` 为父，用修复前的完整工作树快照重建桥接提交：
   **`6435dc2`**（树与修复前 HEAD `6abc24e` 逐字节一致，`git diff` 输出为空）。
3. master 指向新提交；清理 reflog 中指向损坏提交的记录；`git prune` 清除悬空对象。

**修复前后对比：**

| 检查项 | 修复前 | 修复后 |
|---|---|---|
| `git fsck` | 9 个对象 missing，307 行报错 | rc=0，零输出（全干净）|
| `git gc` | 卡死失败 | rc=0 成功 |
| `git show` 历史 | fatal: unable to read tree | 正常输出 |
| 可达对象 | 对象图断链 | 3434 个，0 缺失 |
| 历史提交数 | 断链 | 221 个，连续完整 |
| `.git` 体积 | 13.0 MB（含损坏垃圾）| 4.76 MB（单 pack）|
| 与远端关系 | 无法推送 | 以远端 `ab4ee4f` 为祖先，**快进推送**，无需强推 |

---

## 三、被跳过的 6 个提交（完整说明归档）

这 6 个提交不再出现在 git 历史中（因为它们的快照已不存在），
但**它们合起来带来的全部文件变更，都包含在 `6435dc2` 这一提交里**。

### `84cfadc`

```
----- 84cfadc30f671d33b359b32d61368e1728db2418
2026-09-17 07:48:40 +0800
y168521 <944392324@qq.com>
docs: 登记彩票系统全量数据体检(JS-20260917-01/W63补113)
```

### `78be3f1`

```
----- 78be3f13343bceb32c93718c9bbd27ce4ecb75e3
2026-09-02 18:12:24 +0800
y168521 <944392324@qq.com>
fix: 知识体检改为跨月开机补跑，不再认死1号(JS-20260902-01)

- _task_kb_lint: now.day!=1 守卫 → 读 kb_lint_state.json 比对月份，跨月即补跑
- _write_kb_lint_log: 无条件写日志(status=ok/error+month)，消除"健康=无痕"盲区
- 新增 _kb_lint_state_path/_read_kb_lint_month/_write_kb_lint_month(原子写,损坏容错)
- except:pass → logger.warning，异常不写状态以便下轮重试
```

### `dbced5e`

```
----- dbced5e9e4c84d811b02d5a6f923871c429be8ed
2026-08-26 10:30:55 +0800
y168521 <944392324@qq.com>
fix: 审查子进程sys.path缺失根治(JS-20260826-01/W63补112)

run_review.py+sync_free_models.py被设计为子进程调用，
sys.path[0]=tools/而非项目根→from utils.safe_json import失败→
审查Pipeline returncode=1永久红灯。补_PROJECT_ROOT引导+合并_BA
SE重复赋值。四件套登记齐。
```

### `ace61e5`

```
----- ace61e5efbc16b0df84f201fbf03751979d78845
2026-08-24 16:14:47 +0800
y168521 <944392324@qq.com>
chore: 运行时状态文件解除跟踪+gitignore补漏 (JS-20260824-03)
```

### `9c928be`

```
----- 9c928bed47d1cd3aa23eb13d7916fa7fa4b7e1c2
2026-08-24 15:58:01 +0800
y168521 <944392324@qq.com>
fix: 全库裸写JSON专项治理-生产级13处统一原子写 (JS-20260824-02)
```

### `3968829`

```
----- 3968829b1f9b80ab3c75272e1002cda07ba911db
2026-08-24 15:03:13 +0800
y168521 <944392324@qq.com>
docs: 跨零点重登记JS-20260824-01(原JS-20260823-03)满足今日登记门禁
```

---

## 四、备份与回退

修复前的**完整 `.git`**（含所有当时现存对象、refs、reflog）已整体备份到：

```
E:\坚果云整理_回退备份_20260919\Jinshuiyao_Fixed_git_20260919
```

备份内含：`.git/`（13.2 MB，完整）、`refs.txt`、`reflog.txt`、`fsck.txt`、
`all_commits.log.txt`、`full_messages.txt`（全部提交说明）、`README_备份说明.md`。

**回退方法**：把备份目录里的 `.git` 整个复制回仓库目录，即可恢复到修复前状态。

---

## 五、今后注意

1. **git 仓库不要长期只存在本地**：修好后立刻 `git push`，否则硬盘再坏一次还是全丢。
2. **同步盘放 git 仓库有风险**：本次 13 个松散 blob + 6 个树对象丢失，都是同步环节造成的。
   已按项目铁律关闭 `.pyc` 生成（`PYTHONDONTWRITEBYTECODE=1`）减少小文件数量。
3. 定期 `git fsck` 体检：一条命令就能发现对象缺失。

