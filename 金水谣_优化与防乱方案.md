# ⚠️ 已归档：内容已合并至 `金水谣_录.md` §六（防乱机制）。本文件保留供旧链接跳转，新内容以录为准。
# 金水谣 · 优化与防乱方案（全网方法论 + 项目现状）

> 生成：2026-07-21（WorkBuddy）
> 背景：项目经多模型接力优化（Qoder 搭代码、WorkBuddy 整改/文档/智能体），用户担忧"用各大模型优化→越来越乱"。本方案 = 全网防乱方法论落地 + 项目实测 mess 清单 + 已执行清理 + 待办。

---

## 一、全网有价值的方法论（已搜证）

| 来源 | 核心观点 | 金水谣落地 |
|---|---|---|
| **IBM**（标准化 AI 代码生成） | 全局规则 + 工作区规则；规则当"活文档"，定期审查/合并冗余/删 obsolete | 已分层：启动提示词.txt(全局)+AGENTS.md(项目)+本方案契约/规范 |
| **掘金 Harness**（8 Agent 团队） | 模块契约锁边界；重复动作第 3 次沉淀 Skill；重大决策 ADR-Lite 留痕；改组件留版本号 | → 金水谣_模块契约.md / 内容营销流程可沉淀 Skill / 总索引即 ADR |
| **SegmentFault**（多模型一致性） | 规范文件约束所有模型（命名/异常/注释密度，配正反例）；接口契约锁模块边界（dataclass/interface）；自动化校验 | → 金水谣_编码规范.md / 契约测试门禁=wrapup_check |
| **Vibe Coding Git 边界** | 模块契约代替代码所有权；契约测试作合并门禁 | → 改名检查先查"分域还是冗余"再决定 |
| **OpenSpec/Superpowers/Harness 三层** | OpenSpec 锁需求、Superpowers 锁纪律、Harness 锁协作；先规范→再纪律→后协作 | 当前已有规范+纪律(wrapup_check)，协作层=单用户多模型，靠契约防乱 |
| **内容营销智能体 2026 开源**（MoneyPrinterTurbo 92K★/MIT、ClawReel、OpenStoryline/小红书 Apache2.0、video-content-agent） | 脚本→素材→TTS→字幕→合成→发布 全链路；HITL 审核点；成本优化省 50-80%；Skill 模板批量复用 | 金水谣已覆盖「脚本+知识库」段；视频生产段(免费开源)可作 C 档对外网页扩展 |

**最有价值的 3 条**：
1. **规范文件约束所有模型**（命名/异常/注释 + 正反例）——直接消除"缝合怪"。
2. **接口契约锁模块边界**——杜绝跨模型互改导致的运行时炸。
3. **重复第 3 次沉淀成 Skill**——把"拼装智能体"变成可复用、可分发资产（正是用户视频的核心生意）。

---

## 二、项目实测 mess 清单（按危害排序）

| # | 问题 | 危害 | 状态 |
|---|---|---|---|
| 1 | **启动即造全量快照**写进项目内 `tools/backups/snapshot_<ts>_启动`（每次启动+1，保留5） | 坚果云同步树持续膨胀、"启动"文件夹越积越多 | ✅ **已根治**：auto_backup 重定向到 `%LOCALAPPDATA%/Jinshuiyao/backups`，MAX_BACKUPS=3 |
| 2 | 项目内 5 份启动快照冗余 | 占用同步盘 | ✅ 已删 |
| 3 | 死亡文件 `main.py`+`preload.py`（仅被 archive 里写死旧盘符的死启动器引用） | 误导下一模型以为入口是 main.py | ✅ 已删（auto_backup 关键清单同步移除） |
| 4 | 根目录 `_old_backups_consolidated`（505 文件） | 老备份占空间、看着乱 | ✅ **已清空**（2026-07-21 实测 0 文件，目录已无残留） |
| 5 | 22 个 pytest 失败 | 测试信号失真 | ⏳ 环境/版本债（Python3.14 mock 路径 + pandas3.x 字符串列类型），非改名引入、不影响运行；低优先级 |
| 6 | 多个扁平根文件被 `server` 包 import（`jinshuiyao_router.py`/`operation_log.py`/`audio_toolkit.py`/`auto_audit.py`） | 风格"缝合怪"（不同模型放不同处） | ⏳ 建议：择机迁到 `core/` 或 `utils/` 包内（低风险重构，勿盲动） |
| 7 | 两套交接中心曾漂移 | 下一 AI 读错副本 | ✅ 已并一份权威（根目录），Jinshuiyao_Fixed 内改占位指回 |

---

## 三、已执行的清理（本轮）

1. `tools/auto_backup.py`：快照根目录 `tools/backups/` → `%LOCALAPPDATA%/Jinshuiyao/backups`（与坚果云解耦）；`MAX_BACKUPS` 5→3；`CRITICAL_FILES` 移除死亡 `main.py`。
2. 删除 `tools/backups/snapshot_20260721_20xxxx_启动` 共 5 份。
3. 删除死亡文件 `main.py`、`preload.py`。
4. 新增防乱机制文档：`金水谣_模块契约.md`、`金水谣_编码规范.md`（把全网方法论落成强制规则）。

---

## 四、防乱长效机制（建议常驻）

- **每次 AI 接手先读**：AI协作规范_完整版.md → AGENTS.md(五铁律) → 金水谣_全视角审查清单.md(扫5视角) → 金水谣_模块契约.md → 金水谣_编码规范.md → 工作留痕总索引.md。
- **改动即留痕**：总索引(JS-) + 交接中心(W) + 经验箱；收工跑 `wrapup_check.py`。
- **重复模式沉淀 Skill**：内容营销"提取→学结构→生成文案"流程封装为 Skill（计划 B 档）。
- **定期审查规则**：每季度合并冗余/删 obsolete（IBM 建议），避免规范本身变乱。
- **命中率口径铁律**（2026-07-24 增补，JS-20260724-01/02/03）：任何"命中率/准确率"展示必须显式区分四口径——①直选(位置精确匹配)②组选(3码多重集全中/多球种按红球数分奖级)③平均命中号码占比(verifier)④复式覆盖度(命中号码/开奖号码总数，衡量候选集合覆盖程度)。展示层(健康报告/看板/GUI)口径必须一致：GUI 已加 hit_type 双栏(直选/组选)+覆盖度列对齐看板；禁止再用 min_hit=1(任中1码即算命中)灌水值。改回测引擎判定后须同步搜所有展示层引用(全文 grep "命中率/hit_rate/backtest_latest")，否则"引擎对、展示错"长期并存。回测脚本禁止用硬编码白名单替代动态 Data.is_fresh() 检测（JS-20260724-03 教训：白名单误排3彩种致"假STALE"，实际数据已新鲜）。

---

## 五、待办（等你拍板）

- [x] 删除/移出 `_old_backups_consolidated`（505 文件）— 实测已清空，释放同步盘 ✅
- [ ] 22 测试失败：是否投入修（环境债，建议低优先级）
- [ ] 扁平根文件迁包（低风险重构）
- [ ] 内容营销标签 → 封装 Skill（B 档）→ 部署对外网页（C 档）
- [x] **防乱机制落地**：每月「知识体检 Lint」自动化已真正注册（id automation-1784644448088，每月1号）+ `tools/auto_backup.py` 加 `is_safe_backup_location()` 同步盘隔离守卫、`startup_selfcheck.py` 加「备份目录隔离」自检项（详见 JS-20260721-15） ✅

---

## 六、已落地防乱长效机制（2026-07-21 补充，闭环"纸面→真正运行"）

此前本方案第四节只做了"设计"，机制未真正跑起来。本轮已落实两道真闸门：

1. **每月知识体检 Lint 自动化（真运行）**
   - WorkBuddy 自动化「金水谣/天枢 知识体检 Lint」，id `automation-1784644448088`，`FREQ=MONTHLY;BYMONTHDAY=1`（每月1号）。
   - prompt 自带五项检查（矛盾/孤儿/缺失交叉引用/数据缺口/过时路径）+ 今日日志登记 + 低风险修正。
   - ⚠️ 注意：与 `JS-20260721-03` 在 `core/scheduler.py` 内建的 N3 Lint 任务可能重复，待下次运行时确认合并。
2. **启动备份隔离守卫（代码级 fail-closed）**
   - `tools/auto_backup.py` 新增 `is_safe_backup_location()`：备份目录若在坚果云同步树（含 `Nutstore` 字样）或项目目录内 → 判不安全。
   - `create_snapshot()` 开头 fail-closed：不安全则直接跳过快照、绝不写同步盘。
   - `startup_selfcheck.py`（被 `/api/selfcheck` 调用）新增「备份目录隔离」项，网页"一键功能自检"可见，红字告警。
   - 验证：安全路径=True / 同步盘=False / 项目内=False / `create_snapshot(不安全)`→(None,0) / 自检项 passed。
   - 关联：JS-20260721-15、交接中心 W10。
