# ⚠️ 已归档：内容已合并至 `金水谣_录.md` §二（全视角审查清单）。本文件保留供旧链接跳转，新内容以录为准。
# 金水谣 · 全视角审查清单（多模型协作审查整合层）

> 建立：2026-07-23（WorkBuddy 代接手 TRAE，JS-20260723-41）
> 背景：用户指出"每个 AI 优化前我也会全面审查，但每个模型审查出来的问题都不一样"。根因是各模型的"基因"不同——关注点天然互补，但现在是**各审各的**，没有一张把五视角整合起来的开工前检查单。本清单补这块短板。
> 定位：**协作层**审查清单（WHO 审 + 关注什么）。与既有两层互补，不重复：
> - 技术层：`tools/ai_review_agent.py` 的"审查维度"（并发/数据/安全…代码技术维度）
> - 模式层：`knowledge/pattern_library.json`（15 条 PAT-001~015 失败模式，机器可读）
> - **协作层（本文件）**：5 模型视角整合（谁来审 + 容易漏的盲区）

---

## 一、为什么需要全视角清单

四个模型的盲区是**互补**的，单个模型审查必然有缝隙：

| AI 模型 | 擅长领域 | 审查时天然关注 | 天然盲区（容易漏） | 代表成果（经验箱实证） |
|---------|---------|--------------|------------------|----------------------|
| **Qoder** | 工程实现 / 重构 / UI 配色 | 代码规范、命名一致、调用方同步改完没、重复代码消除 | 跨模块影响、安全边界 | Python 3.14 适配、guide_server 退役、七色闭环配色 |
| **WorkBuddy** | 环境 / 测试 / 根因深挖 | 可运行性、py_compile/ruff、smoke_test 全绿、回测基准对不对、测试隔离 | 长期架构演进、文档体系联动 | 运行环境解耦、测试债清零、彩票 Layer0-1-2 系统性修复 |
| **TRAE** | 架构 / 流程 / 协作规范 | 模块边界、责任划分、流程闭环、安全扫描(SSRF/XSS/密钥) | 细节代码质量、UI 视觉 | 架构统一三件套、责任矩阵+七步验收、AI 代码审查工作流 |
| **豆包** | 方案发散 / 思路开阔 | 可能性探索、替代路径、被否决方案 | 落地可行性、技术细节 | （偏规划，少落地代码） |
| **用户（您）** | 业务 / 体验兜底 | 实际用起来顺不顺、有没有"感觉变差"、中文界面一致 | 技术细节（交给模型审） | 每次优化前全面审查，是最后一道兜底 |

> **核心洞察**：Qoder 的工程视角 + WorkBuddy 的测试视角 + TRAE 的架构安全视角 + 豆包的发散视角 + 用户的体验视角，**五张叠在一起才是完整审查**。漏任一张 = 有缝隙 = 下一个 AI 踩坑。

---

## 二、全视角审查清单（开工前逐行扫）

> 用法：每次优化/改代码前，先扫这 5 行。对**每一个你改动的文件**，自问"这一行的要求我覆盖了吗？"任一遗漏 = 开工前没审全。

| # | 视角 | 负责方（最擅长） | 开工前检查要点（逐条过） | 容易忽略的盲区（重点查） |
|---|------|----------------|------------------------|------------------------|
| 1 | **工程实现** | Qoder | ① 命名合规（snake_case/异常封装/docstring）<br>② 改动的函数，所有调用方同步改完没<br>③ 重复代码消除（同逻辑多处复制？）<br>④ 可变默认参数/import * 等 Python 陋习 | 跨模块影响、安全边界（以为只改自己模块，其实牵连别人） |
| 2 | **测试运行** | WorkBuddy | ① `py_compile` / `ruff` 过了吗<br>② `smoke_test --quick` 7 项全绿吗<br>③ 回测基准对不对（命中判定有没有虚高）<br>④ `pytest tests/ -q` 全量跑了吗（不止单文件）<br>⑤ 测试隔离（碰项目目录了？） | 长期架构演进、文档体系是否要联动改 |
| 3 | **架构安全** | TRAE | ① 跨模块影响评估了吗<br>② 安全边界（密钥泄漏？SSRF？XSS？path 穿越？）<br>③ 模块边界有没有破（私造轮子/过度工程）<br>④ fail-closed（出问题默认拒绝不是放行） | 细节代码质量（以为架构对就行，细节埋雷） |
| 4 | **发散替代** | 豆包 | ① 有没有更简单的方案<br>② 有没有没想过的坑（边界/异常路径）<br>③ 被否决方案写了吗（What NOT to do + 为什么） | 落地可行性（方案炫但实现成本爆炸） |
| 5 | **业务体验** | **用户兜底** | ① 用户实际用起来顺不顺<br>② 有没有"感觉变差"的地方（如预测命中率）<br>③ 中文界面/提示一致吗<br>④ 这一步值不值得做（投入产出） | 技术细节（交给 1-4 视角审，用户只管体验与方向） |

---

## 三、与自动审查层的衔接

本清单是**人/AI 开工前手动扫**的协作层。它和两层自动审查衔接，形成完整防线：

1. **开工前**：扫本清单 5 行（协作层）→ 跑 `tools/ast_checker.py`（技术层 AST）→ 查 `knowledge/pattern_library.json`（模式层）。
2. **改完代码**：走 AGENTS 五步 Pipeline（ruff → AST → smoke → wrapup → 模式库）。
3. **收工前**：`tools/wrapup_check.py` 含「改动联动自动检查」（JS-20260723-41 新增，防修 A 忘改 B）+「高频模式速查卡」（经验收集箱顶部，人读 Top5）。

> 三层互补，任何一层漏了下一层兜底。但**本清单是第一道主动防线**——在写第一行代码前就扫，比事后门禁更早堵缝。

---

## 四、接入点（已在以下位置引用，改了本文件要同步）

- `AGENTS.md` ① 开工必读：增加"先扫全视角审查清单 5 行"
- `金水谣_优化与防乱方案.md` 第四节：常驻机制加入本清单
- `工作留痕总索引.md` JS-20260723-41（本条目）
- `AI协作交接中心.md` W24（本条目）

---

## 五、演进规则（防止本清单自己也变乱）

- 每季度审查一次：合并冗余、删 obsolete（IBM 方法论）。
- 新增视角/检查要点 → 同步更新本文件 + AGENTS.md + 防乱方案。
- 某视角发现的高频坑 → 沉淀进 `knowledge/pattern_library.json`（模式层），双向引用。
