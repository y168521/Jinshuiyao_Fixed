# ⚠️ 已归档：内容已合并至 `金水谣_契.md` §二（模块边界契约）。本文件保留供旧链接跳转，新内容以契为准。
# 金水谣 · 模块边界契约（防多模型接力改乱）

> 来源：全网「多 AI 协作防代码腐化」方法论（IBM / 掘金 Harness / SegmentFault / Vibe Coding Git 边界）收敛成本地规则。
> 核心原则：**用"模块契约"代替"代码所有权"**——任何模型（Qoder / WorkBuddy / 豆包…）改代码前，先读这份契约，只动自己负责的模块边界内的东西，不跨边界改别人的内部实现。
> 配套文档：金水谣_编码规范.md、AI协作规范_完整版.md、AGENTS.md（五铁律）。

---

## 1. 顶层目录职责（谁负责什么，禁止越界）

| 目录/文件 | 职责 | 外部可见接口 | 禁止 |
|---|---|---|---|
| `server/`（包） | **唯一 HTTP 入口** | GET `/`、`/workbench`、`/health`；POST `/api/chat`、`/api/video/ingest`、`/api/memory` 等 | 外部直接 import 其内部 handler；改路由只改 `router.py` |
| `core/` | 纯逻辑层（AI 服务、调度器、文件监控、知识自动沉淀） | 类/函数公开签名 | GUI/服务器直接 import 其内部变量 |
| `jinshuiyao/` | **足球域**实现（fetcher / risk_controller / config / audit） | 本域类 | 与通用域混用 |
| `fetchers/` | 通用数据拉取（彩票等） | `Fetcher` | 与足球域 `jinshuiyao/fetcher.py` 合并（**分域双轨，禁止盲并**） |
| `engines/` | 通用预测引擎 | `Killer`（已内联）、`risk_controller`（格式化校正）、`audit` | 与足球域 `jinshuiyao/risk_controller.py`（风控器）合并 |
| `domains/` | 各业务域（lottery/stock/fund/music…） | 各自 `DomainBase` 子类 | 跨域 import 具体实现 |
| `tools/` | 独立工具（doctor 体检、auto_backup） | CLI 入口 | 被 server 在请求路径内调用 |
| `config.py`（根） | **全局**配置（sync_manager 等） | 全局常量 | 与 `jinshuiyao/config.py`（足球域）混淆 |
| `金水谣数据/` | 知识库 + 日志 + 备份（运行态，非代码） | — | 手改 JSON 索引（用工具） |

## 2. 三条"分域双轨"铁律（改名检查时踩过的坑）

改名大检查曾把以下当成"重复模块"要合并——**它们都是不同业务域的实现，盲并=改行为=高风险**：

1. `fetchers/fetcher.py`（通用 Fetcher）≠ `jinshuiyao/fetcher.py`（足球 FootballFetcher）
2. `engines/risk_controller.py`（格式化校正 `get_corrector`）≠ `jinshuiyao/risk_controller.py`（足球风控 `RiskController`）
3. 根 `config.py`（全局）≠ `jinshuiyao/config.py`（足球域，6 模块大量依赖）

→ **任何"疑似重复"先 grep 全项目引用面，确认是冗余还是分域，再决定。**

## 3. 合并 / 删除前的强制动作

- 删除或重命名任何 `.py`：先 `Grep` 全项目确认引用面（曾漏 `TianshuPlugin`/`TianshuScheduler` 两个类名、曾误判 `main.py` 为入口）。
- 真冗余且零风险才合并：例 `engines/killer.py`（壳）+ `killer_fixed.py`（实现）已安全内联并删壳（JS-20260721-12）。
- 启动安全网 `tools/auto_backup.py` 的快照已重定向到本机 `%LOCALAPPDATA%/Jinshuiyao/backups`（**不再写进项目/坚果云同步树**），可本地恢复，但不再污染同步盘。

## 4. 改动留痕（闭环防乱）

- 任何结构性改动 → 登记 `工作留痕总索引.md`（JS- 编号）+ `AI协作交接中心.md`（W 编号）。
- 重大技术决策 → 写进交接中心第二章（ADR-Lite 三段式：决策/理由/替代方案）。
- 重复动作出现第 3 次 → 沉淀成 Skill（例：内容营销智能体的"提取竞品→学结构→生成文案"流程）。
- 收工前跑 `tools/wrapup_check.py`（门禁 7 项），全绿才算完成。

## 5. 测试目录契约（tests/ 子目录 · 2026-07-22 立）

- `tests/` 下任何测试**自包含**：数据根走注入的临时目录，不碰 `金水谣数据/` 等运行态目录（详见 `金水谣_编码规范.md` §7）。
- 删/改测试文件前，先 `grep` 全项目确认引用面（孤儿测试如 `test_preload.py` 测已删模块，留着只会制造假失败）。
- 新增被测类的"持久化路径"必须设计为可注入（构造参数或配置），否则测试无法隔离 → 视为设计缺陷。
