# 金水谣 · 纲（天层 · 不变纲领）

> 本文件是金水谣项目的**最高纲领**。所有 AI 开工前必须先读此纲，再按需读地层(契)和人层(录)。
> 根原则：**系统自净，不假于人**——纪律长在架构上，不长在人的记性上。
> 全文仅 79 行，已吸收原 11 份规范文档 + 道衍推导框架的核心不变部分。

---

## 一、道（根原则）

**系统自净，不假于人**：把所有"做对"的押注从"某个人/会话的自律"搬到"结构的本能"里。
这是金水谣唯一不变的"道"（《道德经》42 章"道生一→一生二→二生三→三生万物"）。

**推导结构**：道（根）→ 一（约束内建）→ 二（阴阳·生守两极）→ 三（天地人·控制环）→ 万物（所有工程实践）。
详见 `金水谣_道衍推导框架_2026-07-27.html`（可视化树，本纲为真源）。

**新增任何铁律必须在留痕中写明推导归属（阴/阳 + 天/地/人 + 知止），否则门禁黄牌。**

## 二、道衍三问推导法（遇新任务先三问，做法自现）

| 问 | 答案模板 | 示例：删文件 |
|----|---------|------------|
| ①阴阳 | 阳(生/建/放/连)还是阴(守/防/收/隔)？若阳，阴守门在哪？ | 阳(清理)，阴守门=salvage+FORBIDDEN |
| ②三才 | 天规划了吗(先写目标态)？地隔离了吗(执行边界)？人复盘了吗(反事实自检)？ | 天=列要删啥；地=salvage+仓库外拒绝；人=删后复盘登记 |
| ③知止 | 边界在哪？什么绝不可碰(FORBIDDEN)？越界怎么被否定？ | 不碰系统目录/仓库外，越界exit 3 |

口诀：**遇事不决三问：阴阳(生守)→三才(天地人)→知止(FORBIDDEN)。做法自现。**

## 三、八高铁律（任何会话、永久生效、无例外）

1. **开工必读**：先读 `AI协作交接中心.md` + 本纲 + `金水谣_契.md` + `金水谣_录.md`，禁止盲开工。换机器时额外执行交接中心环境检查，且必须先 `git pull`（或双击 `同步代码.bat`）确保本地是最新代码。
2. **收工必写**：更新交接中心 + 追加经验收集箱，缺一=白干。
3. **细致留痕**：每次工作结束前在 `工作留痕总索引.md` 登记一条（编号+经手AI+改动文件绝对路径与行号+验证+遗留+关联），必含三字段：被否决方案/人工介入触发/知识成熟度。
4. **严守架构**：http.server + 多域GUI原生架构，禁止过度工程/私造轮子/新增无用依赖。
5. **收工自检**：说"完成"前必须跑 `py -3.14 tools/gate.py --check`（gate.py 是纯调度器，用 `py -3.14` 即可，不涉及第三方依赖），全绿才能收工。红灯=未完成，修完重跑。**注意**：`py -3.14 -m pytest tests/` 跑测试必须用 venv python（%LOCALAPPDATA%\Jinshuiyao\venv\Scripts\python.exe），系统 Python 缺第三方包会导致 50+ 失败。
6. **收工必存**：所有源码/文档改动必须 `git commit` 入库 + `git push` 到 GitHub 远程（不是只写登记），漏commit=接力断点，漏push=远程落后。换机前先 `git pull`（或双击 `同步代码.bat`）确保双机一致。
7. **风险入册**：外部易变依赖须先入 `金水谣数据/risk_register.json` 再动手，不得裸奔。
8. **反事实诚实**："预测/彩票类"决策卡须填三字段：counterfactual_baseline / baseline_comparison / honesty_note。

## 四、代码审查 Pipeline 铁律

改完代码后必须依次跑 5 步审查，全过才能说"完成"：

| 步骤 | 命令 | 跳过=作弊 |
|------|------|-----------|
| Step 1 ruff | `ruff check . --config pyproject.toml`（仓库根执行，勿重复 Jinshuiyao_Fixed/ 前缀） | ❌ |
| Step 2 AST | `py -3.14 tools/gate.py --ast --quick` | ❌ |
| Step 3 smoke | `py -3.14 tools/gate.py --smoke --quick` | ❌ |
| Step 4 gate | `py -3.14 tools/gate.py --check` (36 项) | ❌ |
| Step 5 audit | `py -3.14 tools/gate.py --audit` (跨文档一致性·6项) | ❌ |
| Step 6 AI review | `py -3.14 tools/gate.py --review` (可选，大改动建议跑) | ⚠️ 建议 |
| Step 7 模式库 | 新发现模式写入 `knowledge/pattern_library.json` | ⚠️ 建议 |

口诀：**改完代码七步走：ruff → AST → smoke → gate → audit → review → 模式库。红灯=没做完，跳步=作弊。**

## 五、安全发布铁律（对外发布/网络暴露时必读）

1. **禁止绑 0.0.0.0**：`ThreadingHTTPServer(("127.0.0.1", PORT), ...)` 仅本机。
2. **CORS 禁止 \***：同源反射，不放行任意域名。
3. **敏感端点同源校验**：`/open` 等加 Origin 校验，跨域 403。
4. **SSRF 受控重定向**：URL 代取逐跳校验 ≤5 跳，拒内网/云元数据。
5. **对外发布前必补**：AUTH_TOKEN 登录、HTTPS、限流、审计、CVE 扫描。

## 六、数据三层隔离铁律

数据按 活(Live)/副本(Replica)/保险(Insurance) 三层物理隔离：
- **活层**=`金水谣数据/`（运行时，可重建）
- **副本层**=`金水谣数据/backups/`（仅备份脚本可写）
- **保险层**=`git 真源` + `金水谣数据/insurance/`（只读）

共享高频文件写前必须 `LeaseManager.acquire_for_write` 占锁；演示/真实数据分文件命名（`_real`/`_demo`/`_supplemented`）。

**三层互写开关**：Layer A(活→副本) 走 `scripts/layer_registry.py copy_up/down`；Layer B(副本→保险) 人工 git add。双会话互写必须 `lease_helper.py acquire`。

## 七、编号命名规则

| 前缀 | 含义 | 示例 |
|------|------|------|
| JS | 金水谣工作留痕 | JS-20260729-01 |
| TS | 天枢工作留痕 | TS-20260719-01 |
| R- | 风险登记册 | R-001 |
| W | WorkBuddy 完成项 | W12 |
| T | TRAE 完成项 | T3 |

---

## 八、自动化优化与成本治理铁律（自主进化·不可烧穿预算）

> 金水谣的 AI 路由/模型选择必须能"自己变聪明、自己省钱"，但**任何自主行为都必须在熔断闸内**——自主路由没有熔断，就是一颗昂贵的炸弹。

**道衍推导**：①阴阳：阳=模型自主进化（影子测试→自动晋级更便宜模型）；阴=成本闸+限流+熔断死守钱袋子。②三才：天=所有上限外部化（`config/*.json`，不写死）；地=优化器模块与业务代码隔离，路由层只调用不耦合；人=遥测落盘可查、跳闸写日志可复盘。③知止：单日/单分/单笔三重成本上限、全局流量突增≥500%即跳闸、无限流/无预算/无超时/无廉价 fallback = **禁止上线**。

**八条铁律（G1-G8，任何会话永久生效）**：

1. **成本熔断（G2）**：任何付费 API 调用前必须过 `get_guard().allow_paid()`；超日/分/单笔上限即跳闸，强制降级免费，绝不静默烧穿预算（配置 `config/llm_budget.json`）。
2. **健康闭环（G1）**：免费模型池读取 `金水谣数据/free_model_status.json`，`down` 跳过、`degraded` 降级；状态超 3h 不信任 → fail-safe 全量可用。
3. **请求限流（G3）**：`server/rate_limiter.py` 每 IP 令牌桶（突发10/0.5每秒补充）+ 全局异常流量≥500%突增跳闸；本机 `127.0.0.1` 永远放行，`/health` 豁免。
4. **统一遥测（G6）**：每次 LLM 调用成本/延迟/令牌数落 `金水谣数据/telemetry.jsonl`，无遥测=无法优化。
5. **并发门（G5）**：`core/model_router.route()` 经 `ConcurrencyGate` 信号量背压，超 `max_concurrent` 返回 `BUSY_OVERLOAD`，不堆积不雪崩。
6. **调用级熔断（G7）**：LLM 调用包 `CircuitBreakerRegistry`，连续失败达阈值即 OPEN 跳过死模型，冷却后 HALF_OPEN 探活。
7. **超时+廉价 fallback（G8）**：外部调用严格 `timeout` + 重试上限 + 指定更便宜 fallback；**禁止无界重试/无限循环**。
8. **影子测试（G4）**：新模型先 5% 影子流量 + LLM-as-Judge 评分，达标才可晋级；`auto_promote` 默认 false（只建议不自动切），防误升劣化模型。

> 关联：`金水谣_契.md§三.5`（优化器机制契约）· `金水谣自动化优化方案.html`（G1-G8 诊断与落地）· `core/llm_budget.py` / `server/rate_limiter.py` / `core/telemetry.py` / `core/concurrency_gate.py` / `core/model_shadow.py`

---

## 九、对抗 AI 惰性 / 闭塞 · 五道防线铁律（JS-20260727-07 · 用户深层要求，任何会话永久生效）

> 用户指出：文字越多，AI 越像人一样"惰性略过"——只简单扫一眼、走过场、缺乏认真思考；且只在内部文档打转会"信息闭塞"。本铁律与证据制度互补：证据制度管"做没做"，本铁律管"读没读全 + 想没想透 + 信息闭不闭塞"。

**五道防线（红线，无例外）**：
1. **检索前置**：动手前先读全关键上下文（交接中心+总索引+经验箱+相关源码），禁止凭印象/内置记忆直接下结论。
2. **引用门禁**：任何结论须引用证据（文件:行号/命令输出/数据）；涉及外部事实/技术结论/模型有效性的断言须附联网核验来源，否则标【待联网核验】回退。
3. **原子化**：大文档/大任务拆小单元逐个处理，一次最多领 2-3 个。
4. **濯石淘洗（省成本·已落地）**：硅基流动免费模型 `THUDM/GLM-4-9B-0414` 已落地于项目代码内部（`ai_review_agent` 经 `core/free_model_pool` 用其替代 DeepSeek 省审查费，零成本）；WorkBuddy 自动化接不进硅基流动，维护脏活 LLM 已回退 `auto` 待确认免费款。换/接新免费模型只改 `config/free_models.json`，不动代码。
5. **主动抽考（兜底）**：收工/接力随机抽考当日文档细节，答不出强制重读（按 录·收工六步 执行）。
6. **不闭塞（抗信息孤岛）**：结论可能受外部变化影响（模型有效性/法规/API 或依赖变动/行业监管）时先联网核验再下结论，禁止闭门造车（jinshuiyao-verify 先核实再结论）；周常联网吸收外部新知打破内部闭环。

> 关联 Skill：`jinshuiyao-verify`（动手前强制核实）· 模型池 `core/free_model_pool.py`

---

> 关联文件：`金水谣_契.md`（执行契约）· `金水谣_录.md`（过程记录）· `AI协作交接中心.md`（进度）· `工作留痕总索引.md`（溯源）
