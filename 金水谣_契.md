# 金水谣 · 契（地层 · 可执行契约）

> 本文件是金水谣项目的**执行契约**。编码规范、模块边界、视觉体系、部署约定都在这里。
> 上层原则见 `金水谣_纲.md`，过程记录见 `金水谣_录.md`。

---

## 一、核心文件地图

| 用途 | 路径 |
|------|------|
| 服务入口 | `启动金水谣助手.bat` → `launch_jinshuiyao.py` → `server/__init__.py`（端口18888） |
| AI体（薄委托层） | `core/ai_agent.py` |
| AI多智能体管线 | `core/agent_orchestrator.py`（RouteAgent→WorkerAgent→ReviewAgent） |
| AI向量记忆 | `core/agent_vector_memory.py`（n-gram语义搜索，无外部依赖） |
| AI工具注册 | `core/agent_tools.py`（@tool装饰器+自动发现） |
| 知识库调度 | `core/dispatch_knowledge.py` |
| AI服务 | `core/ai_service.py`（DeepSeek，requests连接池） |
| 自动知识核心 | `core/auto_knowledge.py` |
| 路由 | `server/router.py` + `server/handlers/` |
| 成本闸 | `core/llm_budget.py` |
| 限流 | `server/rate_limiter.py` |
| 遥测 | `core/telemetry.py` |
| 并发门 | `core/concurrency_gate.py` |
| 影子测试 | `core/model_shadow.py` |
| 数据 | `金水谣数据/`（JSON业务数据） |
| 收工门禁 | `tools/gate.py --check` |
| 运维工具 | `tools/ops.py` |
| 一键同步脚本 | `同步代码.bat`（双向pull+push，双机通用） |
| 台式机设置说明 | `README-台式机设置.txt`（供WorkBuddy部署使用） |
| 项目结构整理 | `tools/reorg.py`（一键重排脚本，--dry-run/--apply/--rollback） |
| 前端统一目录 | `frontend/`（九个子系统：lottery/fund/stock/football/dashboard/gap-analysis/guide/quant-dashboard/trend + _shared） |
| 前端页面注册表 | `config/page_registry.json`（25页全登记） |
| 配置汇总（参见§七） | `config/` 7个JSON + `config.py` + `config/path_resolver.py` |

## 二、模块边界契约

| 目录/文件 | 职责 | 外部可见接口 | 禁止 |
|-----------|------|-------------|------|
| `server/` | 唯一 HTTP 入口 | `router.py` 中路由 | 外部直接 import 内部 handler |
| `core/llm_budget.py` | 付费成本熔断闸（单例） | `get_guard().allow_paid()` / `.record()` / `.status()` | 业务代码直接读 `_daily_spent` 等内部状态 |
| `server/rate_limiter.py` | 每IP令牌桶+全局异常流量跳闸 | `rate_limiter().allow(ip)` | 路由层以外的地方调用；本机 IP 被限流 |
| `core/telemetry.py` | 统一遥测 sink（落盘 jsonl） | `record(**fields)` / `summary(n)` | 遥测线程阻塞主请求路径 |
| `core/concurrency_gate.py` | 并发信号量背压 | `acquire(timeout)` / `release()` | 持锁做 IO；绕过 gate 直接打模型 |
| `core/model_shadow.py` | 影子测试+LLM-as-Judge 评分 | `maybe_shadow(...)` / `shadow_summary()` | 影子流量影响生产延迟；`auto_promote=true` 自动切模型 |
| `core/` | 纯逻辑层 | 公开函数签名 | 直接 import 内部变量 |
| `jinshuiyao/` | 足球域实现 | 本域类 | 与通用域混用 |
| `fetchers/` | 通用数据拉取 | `Fetcher` | 与足球域合并（分域双轨） |
| `engines/` | 通用预测引擎 | `Killer`、`risk_controller` | 与足球域合并 |
| `domains/` | 各业务域(lottery/stock/fund/…) | `DomainBase` 子类 | 跨域 import 具体实现 |
| `tools/` | 独立工具 | CLI 入口 | 被 server 在请求路径内调用 |
| `config.py`(根) | 全局配置 | 全局常量 | 与 `jinshuiyao/config.py` 混淆 |

### 分域双轨铁律（三条永不合并）
1. `fetchers/fetcher.py`（通用 Fetcher）≠ `jinshuiyao/fetcher.py`（足球 FootballFetcher）
2. `engines/risk_controller.py`（格式化校正）≠ `jinshuiyao/risk_controller.py`（足球风控）
3. 根 `config.py`（全局）≠ `jinshuiyao/config.py`（足球域）

任何"疑似重复"先 grep 全项目引用面确认是冗余还是分域。

### 合并/删除前的强制动作
- 删除/重命名任何 `.py`：先 grep 全项目确认引用面
- 启动安全网 `tools/auto_backup.py` 快照已重定向到 `%LOCALAPPDATA%/Jinshuiyao/backups`

### scripts/ 目录职责
| 脚本 | 职责 | 调用入口 |
|------|------|---------|
| `closeout_gate.py` | DoD 五查：git状态/索引覆盖/决策卡完整/三层闭环/commit交叉 | `gate.py --closeout` |
| `quality_gate.py` | 质量基线：哈希校验+覆盖度+一致性 | `gate.py --quality` |
| `safe_cleanup.py` | 安全删除：七类分级+三级闸门+salvage兜底 | `ops.py --clean` |
| `data_backup.py` | 数据层活→副本同步 | `ops.py --data-backup` |
| `data_restore.py` | 数据层副本→活恢复 | `ops.py --data-restore` |
| `session_coordinator.py` | 会话租约：防止双会话互写冲突 | `ops.py --lease` |
| `kb_append.py` | 知识库仅追加写入（写锁防护） | `ops.py --append` |
| `layer_registry.py` | 三层数据拷贝（活/副本/保险） | 直接调用 |
| `lease_helper.py` | 写前占锁/写后释放 | 被 layer_registry 调用 |
| `verify_risk_register.py` | 风险登记册完整性校验 | 直接调用 |
| `run_tests.py`(tools/) | 全量测试框架（零pytest依赖） | `py -3 run_tests.py` |

## 三、关键机制契约

### 3.1 同频共识·多会话占锁机制

**问题**：多 AI 会话同时写同一文件（经验箱/决策卡/scheduler.json）→ 数据丢失/状态损坏。
**方案**：写前占锁 + 仅追加 + 事后协调。

- ⚠️ **AI 手改 4 共享文档（交接中心/经验箱/总索引/契）须先 `session_coordinator acquire` 占锁共识**；程序化写一律走 `utils/shared_write.py` 的 `protected_write_*`（锁失败仍直写不丢数据）。PROTECTED_REL 已含此 4 文档 + ai_decisions + MEMORY（JS-20260806-07 补全）。

| 层 | 工具 | 职责 |
|----|------|------|
| 写前占锁 | `scripts/session_coordinator.py` | 全局锁文件 `.workbuddy/session_lock.json`，acquire/release，超时 24h 自动解锁 |
| 仅追加 | `scripts/kb_append.py` | 知识库写操作只追加不覆写，天然防冲突 |
| 事后协调 | `core/scheduler.py` 内 `_task_knowledge_extract` | 定期扫描 ai_decisions.md，增量提取到知识库 |

**铁律**：操作共享文件前先 `ops.py --lease` 确认写权；任何 `safe_write_json` 必须用 `embed_checksum=False`。

### 3.2 安全删除框架（七类划分 + 三级闸门 + SALVAGE）

由 `scripts/safe_cleanup.py` 实现。文件先分七类，再按类决定闸门级别。

| 分类 | 内容 | 闸门 |
|------|------|------|
| A runtime_junk | 运行时日志/缓存（*.log, *_audit.log*, _kb_backup_*.json, watchdog*, video_cache） | 自动(`--apply`) |
| B conflict_res | 冲突文件（*冲突*.orig *.rej *.mine *.theirs） | 自动(`--apply`) |
| C dead | 死亡代码（git 无引用/no callers） | `--apply` + salvage |
| D duplicate | 重复副本（同功能不同位置） | `--apply`(确认权威源后用) |
| E unique_doc | 唯一文档/知识（.md.html，git 不可追溯） | **FORBIDDEN** + `--confirm-unique` |
| F active_ref | 活跃引用（当前系统使用中） | **FORBIDDEN** + `--confirm-unique` |
| G untracked_new | 未提交新文件（git 未跟踪/未见过） | **FORBIDDEN** + `--confirm-unique` |

**安全硬约束**（任何模式下永久有效）：
- 禁止递归 `rm`（只删文件，不删目录）
- 禁止操作 `.git/` 目录内文件
- 禁止操作 git 仓库外的系统目录
- 默认 DRY-RUN（加 `--apply` 才真正执行）
- 被删文件先 salvage 到临时目录（时效保留，循环覆盖）
- 违例 `exit 3`

### 3.3 诚实回测三数检验法

**一句话**：任何"预测命中率"必须同时展示三个数字，缺一不可。

| 指标 | 定义 | 门槛 |
|------|------|------|
| ① 去极值净值 | 剔除最优/最差 10% 后的平均命中率 | ≥随机基线 |
| ② 随机基线对照 | 同口径同样本的随机猜测命中率（如 3 码=30%，直选=0.1%） | 必列 |
| ③ 理论期望 | 赔率隐含的期望命中率（1/赔率） | 参考 |

**被否决方案**：仅展示"总命中率"（`min_hit=1` 时 3 码任中 1 码即算→95-100% 假绿）。
**铁律**：回测报告必须含 ①②③，且用 `_evaluate_hit()` 双档（直选精确/组选容错）分别统计。

### 3.4 定时任务自动化清单

金水谣调度器 `core/scheduler.py` 内置定时任务（JS-20260806-10 校正：原表整段过期，描述的是早期架构，任务名/间隔均对不上）。当前实际注册以 `_register_default_tasks()` 的 `register()` 调用为准，默认共 **15 项原生任务** + `automation_mirror` 平移的脚本任务。间隔外部化在 `config/scheduler.json`（下表缺省值可被用户配置覆盖）。

| 间隔(默认,分钟) | 任务名 | 职责 | 备注 |
|----------------|--------|------|------|
| 60 | `data_refresh` | 抓取最新彩票/股票/足彩数据 | run_now（开机即抓） |
| 120 | `auto_review` | 对最近预测自动复盘 | run_now |
| 120 | `knowledge_extract` | 从复盘自动提取知识卡片 | |
| 1440 | `data_maintenance` | 清理过期数据、压缩归档 | |
| 1440 | `health_backup` | 全量数据备份 | |
| 1440 | `file_cleanup` | 清理临时文件、整理目录 | |
| 1440 | `memory_decay` | 记忆衰减+自动归档 | |
| 1440 | `cross_link` | 双库自动发现链接(MiroFish↔用户库) | |
| 1440 | `kg_rebuild` | 知识图谱重建 | |
| 1440 | `kb_lint` | 知识体检(每月1号真执行,每日触发) | |
| 1440 | `vector_index_rebuild` | 离线VSM语义索引重建 | |
| 1440 | `ai_code_review` | AI代码语义审查(免费模型优先) | config=0 禁用(默认开) |
| 1440 | `free_model_sync` | 免费模型清单自动同步+探活+排序 | config=0 禁用(默认开) |
| 120 | `free_model_health` | 免费模型健康探活 | config=0 禁用(默认开); run_now |
| 30 | `proactive_reminder` | 主动提醒引擎(扫描到期提醒入队) | |
| 由 `automation_mirror` | 平移脚本任务 | 原 WorkBuddy 平台定时(免积分) | 失败仅告警,隔离 |

> 注：表中"config=0 禁用"的三项在 `config/scheduler.json` 默认 >0，即默认全部注册；`register_mirrors` 注册的脚本任务数量随 `scripts/` 配置浮动，不计入上述 15 项。调度器注释曾写"10项/11项原生"，已于 JS-20260806-10 同步校正为 15 项。

配置外部化在 `config/scheduler.json`，改间隔不改代码。

### 3.5 自动化优化与成本治理机制契约（G1-G8）

| 机制 | 模块 | 职责 | 外部化配置 |
|------|------|------|-----------|
| G2 成本熔断 | `core/llm_budget.py` | 单例 `LLMBudgetGuard`：日/分/单笔三重上限+跳闸冷却1h；付费前 `allow_paid()` 预检，超阈返回 False 强制降级免费 | `config/llm_budget.json`（daily/per_minute/per_run 上限+单价） |
| G3 请求限流 | `server/rate_limiter.py` | 每 IP 令牌桶（突发10/0.5每秒）+ 全局滑动窗口；QPS 突增≥500% 跳闸；本机 127.0.0.1 放行、`/health` 豁免 | 阈值常量在该文件顶部（需外置可后续搬 `config/rate_limit.json`） |
| G6 统一遥测 | `core/telemetry.py` | 每次 LLM 调用记 ts/provider/model/in/out tokens/cost/latency 到 `金水谣数据/telemetry.jsonl` | 落盘路径固定，格式 append-only |
| G5 并发门 | `core/concurrency_gate.py` | `ConcurrencyGate` 信号量；`model_router.route()` 经它 `acquire/release`，超 `max_concurrent` 返回 `BUSY_OVERLOAD` | `config/model_router.json` 的 `max_concurrent`/`acquire_timeout` |
| G7 调用级熔断 | `core/free_model_pool.py`(`call_ai_failover`) | 每个模型包 `CircuitBreakerRegistry`，连续失败达阈值 OPEN 跳过死模型，冷却后 HALF_OPEN 探活 | `CircuitBreakerRegistry` 默认阈值 |
| G1 健康闭环 | `core/free_model_pool.py`(`_load_health_exclusions`) | 读 `金水谣数据/free_model_status.json`，`down` 跳过、`degraded` 降级；状态超 3h 不信任→fail-safe 全量可用 | `free_model_status.json`（由健康探针写） |
| G4 影子测试 | `core/model_shadow.py` | 5% 影子流量 + LLM-as-Judge 数学评分；达标可建议晋级，`auto_promote` 默认 false | `config/model_router.json` 的 `shadow{enabled,sample_rate,min_samples,auto_promote}` |
| G8 超时+fallback | `core/model_router.py` | `route()` 默认 `call_timeout_seconds=60`，超时走更便宜 fallback；禁止无界重试 | `config/model_router.json` 的 `call_timeout_seconds` |

**铁律**：① 路由层只调用以上模块、不耦合其内部状态；② 任何付费调用必须过 `allow_paid()`，否则视为烧钱 bug；③ 影子测试 `auto_promote` 永远保持 false（只建议，人工拍板才切）；④ 遥测是优化前提，禁止关闭。

### 3.6 对外接口安全基线（CORS 同源反射 · SSRF 入口校验）

**问题**：审查盘点发现 ①`/status` 与 `quant_server` 两处硬编码 `Access-Control-Allow-Origin: *`，任意跨域网页可读响应；②`video_extractor` 仅校验重定向跳板 host、未校验**初始用户 URL**，SSRF 入口漏防；③`open_local_file` 用 f-string 拼 `explorer` 路径，存在引号/元字符注入歧义。

**方案（JS-20260806-09）**：
- ⚠️ **CORS 一律同源反射，禁止 `*`**：所有对外 JSON 接口经 `router._set_cors()`（仅当 `Origin` 与自身同端口才回显）；`/status` 删 `*` 改委托 `_set_cors()`；`quant_server` 新增 `_is_same_origin`/`_set_cors` 镜像同一逻辑。同源反射比 `*` 安全，且不挡本机同源前端（前端同源）。
- ⚠️ **SSRF 校验必须覆盖"入口 URL"而不仅是重定向跳板**：`_request_with_retry`/`_post_with_retry` 在首个 GET/POST 前先 `_is_safe_host(url)`，拒绝环回/私网/链路本地/保留地址；重定向逐跳校验早已存在（勿重复造轮子）。`_is_safe_host` 解析异常 fail-closed 返回 False。
- ⚠️ **启动外部进程用参数列表，不用 f-string**：`subprocess.Popen(['explorer.exe', path])` 消除引号/元字符注入歧义（同文件已有 `['explorer.exe', vbs]` 先例）。

## 四、编码规范

### 命名约定
- 函数/变量：`snake_case`（如 `calc_kill_rate`）
- 类：`PascalCase`（如 `JinshuiyaoScheduler`）
- 常量：`UPPER_SNAKE`（如 `MAX_BACKUPS`）
- 禁止：中英混写类名、拼音缩写当变量
- 文件名：模块 `snake_case.py`；测试 `test_*.py`

### 异常处理
- 关键路径必须 `try/except`，禁止裸 `except:` 吞错
- 启动类异常不得静默退出
- 外部调用用超时+熔断

### 注释
- 公开函数/类：docstring（参数+返回+职责）
- 内部逻辑：只在"为什么"难懂处注释

### 前端 SPA 特规
- 改完必须 `node --check` 提取出的内联 `<script>`
- 插入新功能用唯一锚点，每处断言只命中一次

### 依赖与路径
- 第三方依赖只进 `requirements.txt`
- 禁止写死解释器路径，用 `py` 启动器
- 密钥只放 `~/.jinshuiyao-secrets/`

### 测试隔离
- 测试禁止读写项目内持久共享目录
- `setUp` 用 `tempfile.mkdtemp()`，`tearDown` 清理临时根
- 被测类加可选 `root_dir` 参数让测试注入隔离目录
- 禁止用 `@skip` 掩盖失败测试

### 编码铁律速查
1. `safe_write_json` 必须 `embed_checksum=False`
2. GUI文件必须注册到 `gui_files` 列表
3. 新页面必须加 `_PAGE_ROUTES`
4. 终端Emoji用 `_safe_icon()` 处理
5. 不硬编码路径，用 `config/paths.json`
6. 能复用绝不新写，能极简绝不堆砌
7. 所有实现自带 try/except
8. 改完用 venv python 跑 `pytest tests/ -q`

## 五、视觉决策体系（七色规范）

### 四色为骨
- `#0B1A2F` 深海墨蓝 — 主背景
- `#162840` 深蓝灰 — 卡片/次级背景
- `#E8ECF1` 暖银白 — 主文字
- `#C9A96E` 香槟金 — 强调色/标题/按钮

### 三色为用
- `#5BC0DE` 冰水蓝 — 数据高亮/链接
- `#2D8B7E` 墨绿金 — 正向/成功/在线
- `#C8755A` 赤铜色 — 负向/报错/风险

### 绝对禁止色
红 `#ef4444`、正绿 `#22c55e`、正黄 `#eab308`、紫 `#8b5cf6`、橙 `#f97316`

### CSS 变量
```css
:root{ --deep:#0B1A2F; --card-bg:#162840; --ink:#E8ECF1; --gold:#C9A96E;
  --ice:#5BC0DE; --jade:#2D8B7E; --copper:#C8755A; }
```
所有页面颜色只通过 CSS 变量引用，禁止写死色值。
- ⚠️ **生成器/模板内联色值同受禁**（含 `.py` 模板正文与自动生成的 `金水谣数据/fund_reports/*.html`）：`tools/closeout_gate.py` 第 5 项「禁用色合规」门禁覆盖 `.html/.css/.py`，改完即被守，禁止色零容忍（JS-20260806-01 重新加回）。

## 六、多机部署与同步边界

### 一句话
源码走坚果云自动同步 + **GitHub 远程备份**；venv 每台机器本地建在 `%LOCALAPPDATA%\Jinshuiyao\venv`（与坚果云解耦，防止同步覆盖 pyvenv.cfg）。GitHub 远程仓库：`git@github.com:y168521/Jinshuiyao_Fixed.git`

### 同步边界
| 走云同步 | 本机不同步 | GitHub 远程 |
|---------|-----------|------------|
| 源码 `Jinshuiyao_Fixed/` | venv（~600MB）在 `%LOCALAPPDATA%/Jinshuiyao/venv` | 全量源码（git push/pull） |
| 文档、留痕 | 运行日志/备份 `%LOCALAPPDATA%/Jinshuiyao/` | 仅 git 管理的文档 |
| `requirements.txt` | 密钥 `~/.jinshuiyao-secrets/`（禁止进同步盘） | 不包含密钥 |

### 一键同步脚本
根目录 `同步代码.bat` 自动完成：`git pull --rebase --autostash` → `git add/commit/push`，自动检测 `git` 路径（`E:\Git\cmd\git.exe` 或系统 PATH）。笔记本已设每小时定时任务（任务名：`Jinshuiyao自动同步`）。台式机部署步骤见 `README-台式机设置.txt`。

### 启动器自动建 venv
`launch_jinshuiyao.py` 的 `ensure_runtime()` 自动探测 `py -3.14` 并建 venv + pip install。
换机后不要手改 pyvenv.cfg，让启动器自愈。
历史旧路径 `D:\Project_Env\jinshuiyao_env` 已废止，所有新安装走 `%LOCALAPPDATA%`。

## 七、配置一览（单域单文件，无重叠）

| 域 | 文件 | 格式 | 说明 |
|----|------|------|------|
| 全局常量 | `config.py`(根) | Python | 版本号、彩种规则、引擎名称、预算限制 |
| 路径 | `config/paths.json` | JSON | Python/FFmpeg 路径候选 |
| AI 模式 | `config/ai_mode.json` | JSON | AI 运行模式（NORMAL/DEGRADED/OFFLINE/OVERRIDE） |
| 调度间隔 | `config/scheduler.json` | JSON | 各定时任务的间隔分钟数(可被覆盖,非固定13项) |
| 模型路由 | `config/model_router.json` | JSON | DeepSeek/免费模型的路由配置 + shadow/超时/并发门参数 |
| 成本预算 | `config/llm_budget.json` | JSON | 付费 API 日/分/单笔成本上限 + 单价（G2 成本熔断） |
| 免费模型池 | `config/free_models.json` | JSON | 硅基流动免费模型列表 |
| 主题 | `config/themes.json` | JSON | 七色视觉体系 CSS 变量值 |
| 路径解析 | `config/path_resolver.py` | Python | 运行时路径解析逻辑 |
| 日志 | `config/logging_config.py` | Python | logging 配置 |
| 前端页面 | `config/page_registry.json` | JSON | 25 个用户界面页面的中央注册表 |
| 密钥路径 | `~/.jinshuiyao-secrets/` | 文本 | 项目目录外的安全密钥目录 |

每个域一个文件，无重叠。`config.py` 内的 `LOTTERY_RULES` 可被外部 `rule_config.json` 覆盖（优先级：外部 > 内建）。
