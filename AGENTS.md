# AGENTS.md — 金水谣 AI 工作规范（薄参考版）

> 本项目所有规范已合并为三层文档体系，均在 `模型/` 目录下。
> **开工前必读顺序**：交接中心 → 纲 → 契 → 录 → 总索引

---

## ⭐ 铁律 0：完成即留存（最高优先级，不可违反）

**每个任务做完一件事，当场立刻登记，不许拖到收工。** 本条专门治"做完了但忘了留痕"的病（用户明确要求固化）：

1. **一个任务 = 一条记录**。改动一旦做完并验证，立刻在 `AI协作交接中心.md` 追加一行（格式：`| Wxx | 标题 日期 | opencode | 已完成 | 干了什么+验证结果+⚠️注意 | JS-编号 |`），并在 `工作留痕总索引.md` 汇总表 + 明细补一条（JS-编号格式：`JS-YYYYMMDD-序号`）。
2. **攒够 2 个任务立即 git 提交**，不等收工。提交信息 `docs: 登记...`。
3. **经验/踩坑当场写**进 `金水谣数据/log/经验收集箱.md`（当天新开条目，不攒）。
4. **提交后 30 秒内检查** `git status` 是否还有遗留未提交的源文件，有就补交。
5. 收工前再过一遍交接中心，确保**今天做的每件事都有 JS 编号**。

> 判定标准：一个任务从"完成"到"登记"之间，最多只允许隔一个 git 提交的操作。

---

## 文档体系

| 层 | 文件 | 内容 | 必读程度 |
|----|------|------|---------|
| 天·纲 | `金水谣_纲.md` | 五铁律(8条)、道衍三问推导、代码审查Pipeline、安全发布、数据隔离 | ⭐⭐⭐ 开工必读 |
| 地·契 | `金水谣_契.md` | 模块边界契约、编码规范(11铁律)、七色视觉体系、多机部署 | ⭐⭐⭐ 改代码前必读 |
| 人·录 | `金水谣_录.md` | 接手SOP、全视角审查清单、流程、防乱机制、质量保障 | ⭐⭐⭐ 收工前必读 |
| 进度 | `AI协作交接中心.md` | 已完成的优化、待办、环境、分工 | ⭐⭐⭐ 开工先读 |
| 溯源 | `工作留痕总索引.md` | 所有变更的历史倒查 | ⭐⭐ 按需查询 |

## 新AI启动流程 (必读)

> 首次接手本项目的 AI，请按以下顺序执行：

1. **读交接中心**: `模型/AI协作交接中心.md` — 当前目标、已完成、待办
2. **读纲**: `模型/金水谣_纲.md` — 五铁律、审查Pipeline
3. **读契**: `模型/金水谣_契.md` — 编码规范、模块边界
4. **读录**: `模型/金水谣_录.md` — 接手SOP、质量保障
5. **跑开工令**: `py -3.14 tools/ops.py --start` — 自动体检 + 显示最近工作
6. **读懂议程**: `tools/agenda.md` — 关注点指引
7. **查总索引**: `模型/工作留痕总索引.md` — 历史变更溯源

## 导航完备性检查（2026-07-29 加入）

> 本项目有 **4 个子系统**（彩票/基金/股票/足彩），各自的目录结构和 Hub 页面：

| 子系统 | 目录 | Hub 路由 | 仪表盘路由 |
|--------|------|----------|-----------|
| 彩票 | `lottery/` | `/lottery` | `/lottery/dashboard` |
| 基金 | `fund/` | `/fund` | `/fund/dashboard` |
| 股票 | `stock/` | `/stock` | `/stock/dashboard` |
| 足彩 | `football/` | `/football` | `/football/dashboard` |

> 创建或修改子系统页面后，必须检查以下入口是否同步更新：

| 检查点 | 路径 | 说明 |
|--------|------|------|
| 控制中心 | `jinshuiyao-guide/control-center.html` | 各子系统卡片内的 web-tools 区域 |
| 子系统 Hub | `xxx/xxx-hub.html` | Hub 页面必须含所有子功能链接 |
| 导航指南 | `jinshuiyao-guide/jinshuiyao-guide.html` | 各子系统对应的表格段落 |
| 路由表 | `server/handlers/static.py` | `_LOTTERY_ROUTES` / `_SUBSYSTEM_ROUTES` |

**每条新页面创建后，必须在 Hub 页加链接 + 子页面顶部加 back-link 回到 Hub + 路由表注册 + 控制中心/导航指南同步**

## 操作留痕系统 (2026-07-29 新增)

本项目已搭载**全自动操作留痕系统**，所有关键操作自动记录，无需手工。

| 触发点 | 自动记录 | 可见于 |
|--------|---------|--------|
| `ops.py --start` | 开工事件 | 浏览器: `/audit-dashboard` |
| `git commit` (pre-commit) | 提交事件 + 文件列表 | 同上 |
| `closeout_gate.py` | 门禁通过/拦截 | 同上 |
| `ops.py --close` | 收工事件 + 今日回放 | 同上 |
| `tools/compliance.py` | 合规报告 | 控制台 + 交接中心 |

**浏览器看板**: `http://localhost:18888/audit-dashboard` — 全中文，无需懂代码/英语

**合规督察**: `py -3.14 tools/compliance.py --out` — 报告自动追加到交接中心

## 收工强制动作

1. 更新 `AI协作交接中心.md` 已完成的优化表格
2. 追加 `金水谣数据/log/经验收集箱.md`
3. 跑 `py -3.14 tools/gate.py --check`（全绿=完成）
4. `工作留痕总索引.md` 登记一条
5. 跑 `py -3.14 tools/ops.py --close`（自动记录收工 + 今日回放）
6. `git commit` 入库（pre-commit 自动记录 + 门禁检查）

## 环境

- Python: `%LOCALAPPDATA%\Jinshuiyao\venv\Scripts\python.exe`（自动建，**跑测试必须用这个**）
- 启动: 双击 `启动金水谣助手.bat`（端口18888）
- 测试: 使用 venv python 跑 `-m pytest tests/ -q`（约814项）
- 收工: `py -3.14 tools/gate.py --check`

## 知识网关（2026-08-02 加入）

> 任何 AI（opencode / Claude Code / Cursor / 网页助手）接入项目知识库的统一入口：
> **先读 `知识网关索引.md`**（全资产清单+检索入口），再用 `core/knowledge_gateway.py::search` 或 MCP 检索。

| 入口 | 说明 |
|------|------|
| 知识网关索引 | `知识网关索引.md`（仓库根，`tools/gen_knowledge_index.py` 自动生成） |
| 四源召回（代码） | `core/knowledge_gateway.py` — search()/summarize()，BM25+图谱+向量+经验+项目文档 |
| 四源召回（HTTP） | `GET /api/knowledge/gateway?q=xxx&limit=8`（服务器 18888） |
| MCP 服务 | `tools/knowledge_mcp.py`（stdio JSON-RPC，Claude Code/Cursor 接入，见 `knowledge-mcp.md`） |
| 经验箱直读 | `金水谣数据/log/经验收集箱.md`（L1 原始层，`## 日期` 标题） |

**使用规则**：遇到报错/异常先查经验箱（90% 的坑有记录）；问题相关时 AI 助手会自动注入网关上下文（`core/ai_agent.py` 纯聊天路径），无需手动。

## 用户沟通

- 全中文回复，通俗解释，不甩代码不甩英文
- 直接推荐最佳方案
- 操作结果给明确反馈
